# Investigation log

One row per WP/anomaly investigation — the feedback loop for whether the
evidence-first process (`scripts/wp_diff.py` + the `wp-investigate` skill) is
actually reducing misdiagnoses. Append a row at the end of every
investigation, including clean ones. Review the trend occasionally; a repeat
failure mode here means a layer (skill trigger, tool output, docs) needs
fixing — not another paragraph of discipline prose.

Columns: **corrected?** = did the owner have to push back / correct the
attribution before it was right (`no` / `yes — what was wrong`).

| date | question (short) | first attribution | final attribution | corrected? |
|---|---|---|---|---|
| 2026-07-09 | Norsemen −17pp drop | "Trout benched" (salient, unweighed) | ~half bullpen K collapse (only flipped cat), ~half Trout | yes — attribution, slot semantics (17=IL not bench), impact overclaim (~15pp vs ~0.4pp), recanted a verified fix under date pushback |
| 2026-07-11 | Bus +12pp / Dawgs −12pp @ ~6am (7/11) | "double-count of a completed Friday game", then hand-waved "schedule catch-up" | doubleheader under-count bug — MIL@PIT 7/10 postponement folded into a 7/17 DH the hitter optimizer counted as 1 game (`_hitter_days_slotted` per-date max); fixed → sum | yes — two wrong mechanisms before the owner's postponement→doubleheader clue |
| 2026-07-16 | m85 −33.7pp swing (7/12) | ran wp_diff.py first — no pre-tool claim | sleep-gap catch-up (193-min gap → first post-wake tick spiked then settled) + R leader-flip Bus→Dawgs + whole-slate re-projection; benign | no — tool-first; wp_diff surfaced the gap + leader-flip |
| 2026-07-20 | m96 (wk16 Bears–Norsemen) +5pp to 20.6% this morning | ran wp_diff.py first — no pre-tool claim | overnight week-16 probable announcements (fetched 04:02/07:40 UTC) re-shuffled SP units at the 06:02 UTC medium recompute: Bears gained Cole/Williams/Seymour announced starts (net ~+1.9 units) vs Norsemen's +Singer −share on 4 others (~0 net) → K win share 10→32%, QS leader flipped (4.38→4.97 vs 4.80→4.41); banked flat, no roster moves in-tick; benign | no |
| 2026-07-22 | did the DH-overlay fix change WAR WP? (m94, WAR=home) | (no pre-tool claim; ran wp_diff) | YES −6.8pp (23.7→16.9): opening Red Sox DH game 2 let Ranger Suárez's cadence start (5.8K/0.6QS) land on it → appears on Shih Tzus (WAR's opp) → K leader-flip + QS. **Correction: I'd twice told the owner Suárez was IL-slot-HARD-EXCLUDED — WRONG.** `_is_playable` sim.py:192 includes IL-slot players with a return estimate (FIFTEEN_DAY_DL) or playable status, gated per-game by return date (ESPN override=today); only OUT/INJURY_RESERVE dropped. Trusted stale CLAUDE.md docstring over code — the exact contract-#3 failure. Owner decision: LEAVE the projection (project likely-activated returners); fixed the stale docstring in 3 places | yes — I claimed IL hard-exclude; owner's framing (he'll probably start) was right |
| 2026-07-22 | WAR increase Tue 18:50→18:55 Oslo (m94 wk16, WAR=home) | first instinct was "first pitch" — rejected after checking: Suarez's game first pitch was 22:40 UTC, ~6h after the drop | phantom-start removal: injured Ranger Suarez (out since early July, ESPN return 7/22, now IL-slotted on the Shih Tzus=WAR's opp) was carrying a stale-anchor cadence start (5.8 K, 0.6 QS) on WAR's opponent; removed at the 16:55 UTC fast tick → K leader flipped to WAR (+21.5pp win share, avg 60.0→54.1) + QS +14.5pp → WAR 22.6%→30.5%; banked flat, no games Final, WAR budget steady. Removal trigger CONFIRMED as mechanism (code-grounded): cadence/flat SP starts only land on OPEN games (no announced probable) — _cadence_extra_start_dist snaps to "next open game" sim.py:894, _open_sp_game_weight skips probabled games sim.py:872. Suarez = BOSTON RED SOX in 2026 (proTeamId 2→MLBAM 111; was PHI earlier — I initially assumed Phillies from real-world memory and wrongly dismissed his correct Red Sox schedule as "incoherent"; owner corrected). Stale anchor (last start 6/29, ~22d), cadence turn snapped to the ~7/24 open Red Sox game; when its probable (Patrick Sandoval) posted at the 16:55 fast tick via refresh-live's ESPN overlay, that game closed and every other BOS wk16 game already had a probable → 0 open games → start drops to 0, budget removed. Team + likely game (7/24 Sandoval) recoverable from current schedule + anchor math; only the exact Tue open/announced transition unrecoverable (team_schedule overwritten). Fast tick confirmed (daily 04:02, medium 14:02/18:02 UTC). LESSON: the 2026 league roster is source of truth, not real-world memory — don't dismiss stored data that contradicts it. Playbook #7 family | yes — team (PHI→BOS); mechanism held | 
| 2026-07-22 | Bus increase Wed 06:00→06:05 Oslo (m92 wk16, Bus=away) | ran wp_diff.py first — no pre-tool claim | probable pickup via daily refresh-schedule (06:02 Oslo=04:02 UTC; starts_recorded=50, espn_probables_filled=83): Cam Schlittler ADDED to Bus budget as projected 7/25 start (last start 7/19, roster slot 14) → QS leader flipped (avg +0.73 = his exp_qs), ERA leader flipped, WHIP toward Bus → Bus 28%→38%; banked flat; legit real start, not phantom | no |
| 2026-07-22 | Dawgs drop Wed 03:35→03:40 Oslo (m91 wk16, corrected from m85/wk15) | first ran wrong week (m85) — user corrected to "this Wednesday"; re-ran m91 | live overshoot: opponent Jo Mamas banked HR 4→6, R 9→11, OPS .94→1.09 in an in-progress game → Dawgs (already underdog ~17%) HR win-share −15pp, OPS/H/R each −10 to −13pp → WP 17%→9.5%; live_recon shows the game being folded in; NOT noise (banked deltas real). Specific HR hitter unrecoverable (live_batters overwritten) | yes — wrong week first (date ambiguity), mechanism right on re-run |
| 2026-07-20 | m85 Dawgs drop Wed 7/15 03:35→03:40 Oslo | ran wp_diff.py first — no pre-tool claim | MC noise: −1.4pp tick inside a ±1.4pp oscillation at p≈0.5 during the All-Star break — banked flat, budgets flat, no flags, no leader flips; also only 1 downsampled point from that window survived to the site chart, so the visible "step" spans wider real time | no |
| 2026-07-20 | why is 13-2 WAR's P(final) 36% vs Norsemen 54% (playoff odds) | evidence-first (per-cat pairwise + season cat records before any claim); first tick-weighted category_state aggregation was garbage, redone via latest_category_state | record ≠ roster: WAR projects elite H (77% vs Po9) but underdog 6/10 cats — K 10% (54 vs 72 proj), SVHD 3% (4 modest RPs = 3.8/wk vs 6 arms = 6.7/wk); season per-cat records agree (K 7-8, ERA 5-10; 13-2 built on batting cats + SVHD); caveat noted: no September streaming/waiver modeling, so pitching-light contenders read worse than their real September selves; benign — worked example added to CLAUDE.md §Playoff odds | no |
| 2026-07-23 | Bus increase Thu ~01:00→03:00 Oslo (m92 wk16, Bus=away) | ran wp_diff.py first — no pre-tool claim | live-play banking, benign: Bus banked its own production across the overnight slate — H 18→23, HR 3→5, R 9→12 (OPS .775→.855 while Bums' OPS regressed 1.09→.96), K 10→15, SVHD 1→2, ERA 9.45→5.54, WHIP 2.10→1.46; QS leader-flipped (Bradish live start, qs-ingame, exp_qs→0.98). Broad (OPS/HR/R/WHIP/ERA/QS/H all moved), no single event. Bus 27.5%→52.4% (+25pp). NOT opponent-overshoot (Bus banked its OWN cats) so likely sticks, though live ERA/WHIP still small-sample | no |
| 2026-07-23 | Knights increase Thu ~01:00→05:00 Oslo (m95 wk16, Knights=home) | ran wp_diff.py first — no pre-tool claim | live-slate two-sided swing, benign: Knights 59%→81% (+22pp). Melonheads' OPS collapsed .738→.643 (OPS flip +22pp win-share); Knights pitching rates filled out WHIP 1.95→1.64 (flip +14) / ERA 5.11→4.30 (+9); Knights banked H 25→31 (+16), SB 1→2 (+21, low-event), R 11→14 (flip +9), SVHD 1→2 (live_recon svhd_added=1, +15). Offset: K −12 / QS −4.9 — Knights lost benched Max Fried's start (benched-live-drop, 5.3K/0.7QS) at ~01:15 Oslo (the early dip to 43%), cushioned because Melonheads' Sandy Alcantara actually PITCHED his 7/22 start (went Final in-window) — a poor line (7IP/5ER/3K, no QS) vs his ~5K/0.55QS/3.91ERA projection, so projection→banked reality helped the Knights in QS/ERA/K (NOT a forfeit like Fried, NOT a phantom like Suárez — he threw, just badly). Overshoot-prone portion = opponent OPS collapse; Knights' own banked counting cats stick | no |
| 2026-07-27 | what drives WAR (10%) & Melonheads (21%) champ odds post-wk16 | evidence-first: seed_dist from playoffs.json + pairwise H2H win% via sample_team_totals + per-cat means | NOT seeding (favors WAR: 90% bye vs Melon 5-6 seed). Bracket roster strength: H2H win% vs the other 5 contenders — Melon 66% (best), WAR 35% (worst). WAR lopsided offense (wins H/SB only; loses K 49v60, QS, SVHD 3.5v4.7, HR, ERA, WHIP) → wins ~2-3 of 10 cats → 13-3 record overstates roster. Melon balanced, above field avg nearly everywhere incl. the pitching cats WAR lacks → high per-game win rate overcomes the extra no-bye round. Same record≠roster theme as 2026-07-20. Caveat: today's rosters for Sept | no |
| 2026-07-29 | any flags? (17 open) | none — checked `last_seen` before attributing anything | **all 17 stale; nothing has fired since 2026-07-27T07:30 (53h, `app validate` runs every 5 min).** All are period 16 (Jul 20–26, decided). Four clusters: (1) **INV_EMPTY_BUDGETS m94/m95/m96, 83 occ, error** — Sunday-night end-of-week drain, verified benign: home budgets fell monotonically 14→0 across Jul 26 20:00–23:30 UTC while away still held 2–4, and all three resolved to the correct winner. The benign-end-of-week gate (validate.py:424) failed to engage; *hypothesis:* `_side_remaining` (validate.py:825) counts non-Final games with **no `now=`** (validate.py:857 → past-date guard off) and **no playability/IL-status filter**, while `build_budgets` applies both, so `active_remaining>0` while budgets are legitimately 0. Exact trigger unrecoverable (team_schedule statuses overwritten in place; the readable period-16 roster snapshot is Jul 27 06:02, *after* the flags stopped). Check-side false positive, not a pipeline failure. (2) **ANOM_STALE_SNAPSHOTS/FETCH** — already-fixed false positive; the fix comment at validate.py:503 names this exact date ("2026-07-27: week 16 decided, earliest-undecided=17 upcoming → spurious"). (3) **ANOM_WP_SWING m95/m96 + ANOM_WP_FLAPPING m95/m92** — genuine end-of-week resolution variance on knife-edge matchups; ruled out the flaky-stat mechanism the check targets: `INV_BANKED_REGRESSED` has never fired and m95's H/K/R were monotonic across the window. (4) **ANOM_RATE_DIVERGENCE m91/m93, 694 occ** — ERA 1.75–2.00 current vs 2.57–2.86 projected at 21–23 IP, just over the 20-IP gate: correct mean reversion, `RATE_DIVERGENCE=0.40` too strict at the low end of the IP range. **All applied same session:** clusters 1–3 resolved with notes; clusters 1 and 4 *fixed in code* (`f31ace2d`) rather than just resolved — `_side_remaining` now loads the schedule with `now=` and filters `_is_playable` so the gate mirrors `build_budgets`, and `ANOM_RATE_DIVERGENCE` gained an absolute floor (`RATE_DIVERGENCE_ABS` 2.50 ERA / 0.80 WHIP). Regression tests `test_side_remaining_*` + `test_rate_divergence_quiet_on_hot_small_sample_regression`; 339 pass. Post-fix: 0 open flags, current-period run clean. **Left alone (pre-existing, not from this change):** 5 `INV_PROJ_LT_CURRENT` on long-finished weeks (m52/m65/m66/m73/m74), each off by exactly 1.0 — a late ESPN stat correction after that week's final snapshot, so `current` climbed past the stored projection. Only visible in an `--all` sweep (fast.sh runs the current period only); cosmetic on dead weeks, but it's the one real leftover if it ever wants tidying. | no |
| 2026-07-30 | Bear Nation increase Thu 06:00→06:05 Oslo (m101 wk17, Bear=away) | ran wp_diff.py first — no pre-tool claim | probable pickup via daily refresh-schedule (04:02:02 UTC, between the two ticks): Gavin Williams (CLE) ADDED to Bear Nation's budget as an announced 8/02 start (units 1.0, exp_k 6.6, exp_qs 0.58) → 4 projected SP starts become 5 → K avg 55.60→62.13 crosses Shih Tzus' 60.16, **K LEADER FLIPPED** (home K win-share 69.1%→37.9%, −31.2pp) + QS 4.22→4.78 (−2.3pp); every other cat ≤1.8pp, home budgets unchanged, banked flat (only Bear OPS .8211→.8107), 13 games went Final at 04:05:02 but contributed no banked movement → Bear Nation 33.7%→42.3% (+8.7pp). Why only the daily job could see it: `refresh-live` (5-min tick) spans yesterday…today+2 = 7/29–8/01 (cli.py:730-732), so 8/02 — the period's last day — is refreshed **once a day** (all 30 rows dated 8/02 carry fetched_at 04:02:02 while 7/29–8/01 were re-fetched at 10:00). Probable-announcement established **by elimination on code**, since team_schedule is overwritten: he's playable (ACTIVE, slot 16 = bench, and bench is NOT filtered — only IL slot 17, sim.py:192) and rostered by Bear Nation since period 9, and CLE's only remaining games are 7/31 Bibee, 8/01 Messick, 8/02 Williams — so if 8/02 had been *open* at 06:00 the cadence walk would have snapped a turn onto it (E[extra] 0.34 from a 7/28 anchor, 1.0 from 7/23; sim.py:878-960 + REST_DAY_WEIGHTS sim.py:215) and if it had *already read Williams* `_probable_starts_for` (sim.py:826) would have given units=1.0 — he had NO line at all at 06:00 (absent from all 16 away budgets), so 8/02 must have carried a different announced probable and the 04:02 refresh replaced it with Williams. `cadence` flag is provenance only (sim.py:1681), not a speculative start: units=1.0 is the announced piece. Legit real start, not phantom; same family as 2026-07-22 Bus/Schlittler | no |
| 2026-08-03 | what happened at the end of wk17 in Sox Teacher–Jo Mamas (m98, Mamas=home)? | ran wp_diff.py first — no pre-tool claim | **one run, top of the 8th, 03:52:48 Oslo.** Mamas were 80.2% at 03:45 Oslo (home_wins 8025/10000) leading 5 cats (R/SVHD/SB/OPS/H) to 5 (HR/QS/K/ERA/WHIP) and holding the hits tiebreaker 59–51, so the whole matchup hung on R: Mamas 37 **fixed** (zero remaining budgets — every one of their games was Final), Sox Teacher 36 banked + ~0.2 expected from the only live game left (BOS@LAD `game_pk` 823919; their last three contributors were Contreras 0.22 g, Durbin 0.22 g, Tanner Scott RP). Caleb Durbin was HBP, moved up on a Monasterio single and a Duran sac bunt, and **scored on a wild pitch by Wyatt Mills at 2026-08-03T01:52:48Z** (during Seigler's AB, which ended in a flyout, `rbi=0`; Monasterio then scored on a passed ball. **Owner corrected an initial "sac fly" reading** — the PA `description` names only the plate outcome, so the manner of a run must come from the runner's own `movement`/`details.event` and `playEvents`, never the description) (statsapi playByPlay; box `r`=1) → R 36→37 = **TIE, LEADER FLIPPED** (home R win-share 76.1%→0.0%, −76.1pp; every other cat ≤0.7pp). A tied cat counts for neither side and only an equal *count* reaches the hits tiebreak (`sim.py:1836-1856`, `TIEBREAKER_STAT_ID = STAT_H` `sim.py:105`), so 5–5+hits became **4–5–1 and Sox Teacher won outright** — the 01:55 tick printed home 80.4%→2.3% (ANOM_WP_SWING −78.1pp), then 0/100. Residual 2.3% = ERA 0.9% + WHIP 1.4% (Tanner Scott, `svhd-ingame`, could still have blown up away's rates). ESPN agrees: winner=AWAY, R=TIE/TIE. Model was **not** wrong — it priced P(away scores ≥1 more) at ~18–20% and the underdog branch hit. Two OPEN error flags on m98 in the same hours, neither outcome-relevant: **INV_EMPTY_BUDGETS ×30** — benign false positive of the *same family fixed on 2026-07-29* but not caught by it: `_side_remaining` (validate.py:866-867) counts any non-Final game for an active-slot playable player, and Yamamoto (LAD, slot 14, ACTIVE) had already made his 8/01 start, so LAD's live 8/02 game kept `active_remaining=1` and the benign end-of-week gate (validate.py:441) never engaged; **INV_SITE_QS_OVERCREDIT ×30 (+204 on 8/02)** — site had home QS=3 vs supportable 2; the sim used 2 and home lost QS either way (vs 4), settled final = 2 | yes — the manner of the run: I read "sac fly" off the PA description; it was a wild pitch. WP mechanism unaffected |
| 2026-08-03 | is the wk17 m98 −78.1pp tick the biggest legitimate 5-min swing we've seen? | ranked all single-tick transitions before judging any of them | **yes — biggest legitimate, 2nd biggest overall.** Scanned all 99,945 consecutive-snapshot transitions with gap ≤6.5 min, same `model_version`, `edited=0` (surviving history only: weeks 1–8 have **no** snapshots at all — the `fad4684` DELETE — so the corpus is period 9 onward, 2026-05-28→now). Exactly two moves beat −78.1pp and **both are documented artifacts where the pre-swing WP was the wrong number**: (1) **m60 −100.0pp** (2026-06-08 06:55→07:00, wk10) — the QS/SVHD double-count; wp_diff shows QS 4.00→3.00 LEADER FLIPPED (SVHD 6→5 too) with **no banked delta**, i.e. the 100% was phantom and 07:00 was the *correction* when `now−7h` aged deGrom's game out of the window (INCIDENTS.md §2026-06-08, fix `83ab98b`, `max(scraped, floor+box)`). (2) **m70 −93.9pp** (2026-06-22 03:55→04:05, wk12) — phantom-start: wp_diff shows K projected 49.69 → banked 44.00 vs away's 45 (LEADER FLIPPED) as Imanaga's `exp_k` 5.7 + four CHC bats were REMOVED with zero banked production; the flag's own resolution note reads "phantom-start artifact (postponed-game Imanaga start suppressed Knights all week, corrected at refresh-schedule)", root cause fixed by the past-date schedule guard. It also spans **10.1 min** (a skipped tick), so it isn't a 5-min tick under any reading. Sensitivity: widening the gap filter to 20 min surfaces nothing else above 20pp. So m98 is the largest swing on record where **the model was right before AND after** — 80.4% correctly priced P(one more BOS run)≈18–20%, the run scored, and a tied R cat flipped 5–5+hits into 4–5–1. *Hypothesis (not decomposed):* the 21:55→22:00 UTC cluster just below it (m50 +35.0, m51 +30.2, m60 +25.5) is midnight CEST — the signature of the host-local `date.today()` lurch (INCIDENTS.md §2026-06-06); all three predate that fix. Also fixed this session: the 200-point/55-min publish downsample that had hidden the m98 cliff from the site chart entirely (`5540ccfd4`) | no |
| 2026-08-03 | do playoff odds include the just-finished wk17 results? surprised Jo Mamas' bye odds aren't higher | evidence-first: read the published payload before answering | **Effectively yes, though the run predates settlement.** Published playoffs.json = 06:02 UTC medium run; ESPN posted wk17 winners at 07:00 UTC (traced via 5-min data.json commits: 0 winners 06:55, 6 at 07:00). Formally wk17 sat in `remaining_matchups`=36 (wk17–22) with 16-game records, BUT all six wk17 matchups were already at 0%/100% home_wp, and the season sim draws `rng.random() < home_wp` (playoffs.py:212) → deterministic at p=0/1. Reconciles exactly: exp_wins = banked W + wk17 result + wk18-22 expectation (WAR 13+1+2.83=16.82; Norsemen 13+0+3.04=16.03; Jo Mamas 11+0+2.16=13.15). Jo Mamas' bye=4.4% is NOT staleness — they LOST wk17 (m98 to Sox Teacher) and it's priced. Real reason: model rates them 8th of 12 rest-of-season (avg 43.2% WP, underdog in 4 of last 5: SwampD 30.8, Norsemen 43.6, ShihTzus 32.8, Bus 35.0, Bums 73.9) → 13.15 exp final wins vs Norsemen 16.03, needing ~3 wins in 5 weeks on one of two teams. Swamp Dragons (9-7) actually edge them on bye odds 4.5 vs 4.4 — same record≠roster theme as 2026-07-27. Flagged unverified: two 5-11 teams (Bus 63.5%, ShihTzus 62.8%) project top-3 ROS, worth a sanity check | no |
| 2026-08-05 | Norsemen WP jump today 08:00→09:00 Oslo (m107 wk18, Norsemen=home) | ran wp_diff.py first — no pre-tool claim; first framing was "live play banking", CORRECTED after reading the recovery tick's log (in_progress=0, boxscore_games=0) — it's a settle boundary, not an hour of baseball | **Daily-settle step, and a league-wide live-tracking failure behind it.** home_wp 43.1% (06:05 UTC) → 79.0% (07:00 UTC), +35.9pp, six leader flips: H +41.6 (45.99→61.12), R +38.7, OPS +33.1, HR +30.7 to Norsemen; ERA −18.1, WHIP −11.6, K −15.8 to Giraffes. Banked deltas = one full day of Tuesday 8/04 baseball landing at once (Norsemen H 4→19, R 2→10, HR 0→3, SB 1→4, AB 24→63; Giraffes H 7→12, R 3→4, K 17→24). Budgets flat both sides, no games Final in-window. **Why it lands in one tick:** the live reconcile (sim.py:500 `reconcile_live_components`) only covers ERA/WHIP components, OPS components, and QS/SVHD — "H & HR stay at baseline — they're scored cats the scrape already keeps live" (sim.py:545). So H/R/HR/SB/K come straight from category_state, which the DOM scrape owns (cli.py:242). **The scrape did NOT keep them live:** every tick 23:00–04:00 UTC logged "live-scraped 120 category cells", yet ALL 12 teams' H was frozen at its 22:00 value through 04:00 and stepped only at the ~07:00 settle (Norsemen 4→19, WAR 4→22, Dawgs 1→10, …). Monday's slate worked (Norsemen H incremented 1→2→3→4 live 00:10–01:25 UTC on 8/04, settle confirmed 4) — so Tuesday is a regression, not by-design lag. *Hypothesis, unverified:* scrape returning stale/wrong cells (ESPN auth-session or page-shape), cf. the 7/29–7/30 ANOM_SCRAPE_EMPTY "check ESPN auth / .playwright_profile" — but it's NOT firing now (cells are non-empty, just stale), so no check catches this. **DEEPENED after owner pushback ("looks out of date, at least for rate stats — but maybe WP uses correct values?"): NO, both input paths were stale, and the rate staleness is DOWNSTREAM of the counting staleness.** Mid-slate live_recon shows `scraped` == `baseline` to 4dp on every rate (home ERA 0.0/0.0, WHIP 0.857/0.8571, OPS 0.3519/0.3519; away ERA 5.17/5.1702, OPS 0.9444/0.9444) — both Monday's numbers. `_judge_group` (sim.py:444) judges the box-score reconstruction by distance to the SCRAPED rate (matched → closer → baseline), so base_err=0 ⇒ nothing can ever be `closer` ⇒ verdict `baseline` every tick, by construction. Both rate groups rejected on both sides for the entire slate (only home OPS briefly `matched` 23:10–23:50). Two sub-cases: (a) the PITCHING recon was probably CORRECT and discarded — ER/OUTS/P_H/P_BB are REST-only, independent of the frozen scored cats (recon ERA 4.91 vs scraped 0.0); (b) the OPS recon was itself CORRUPTED — it adds live AB/2B/3B/BB/HBP/SF but leaves H/HR at baseline by design (sim.py:545), so a fresh denominator over a frozen 4-hit numerator gave OPS 0.198; rejecting it was accidentally right. Impact quantified: projected-final H 45.99→61.12 at settle (+15.1 vs +15 banked) ⇒ the pre-settle projection contained ~none of Tuesday's offense; WP would have climbed toward ~79% through the night instead of snapping. **NOT a regression of the 2026-06-04 evening split-source/single-MAX bug** (owner asked): that fix (`38b4959`, per-(matchup,team,stat) reads) is present and working — db.py:355 still does the per-stat max, and INV_CURRENT_CATS_MISSING/INV_BANKED_REGRESSED correctly stay quiet because no cat is missing or shrinking. That bug made the sim read FEWER cats than the display; today display and sim inputs AGREE exactly (scraped == baseline to 4dp) and are both stale — the fault is upstream of the split, in the scrape's values. **Broken assumption:** `_judge_group` treats the scrape as ground truth for the current rate — holds when the scrape is good OR empty (ANOM_SCRAPE_EMPTY covers empty), fails silently when NON-EMPTY BUT STALE. No check tests scraped values for movement during a slate. Proposed guard: flag when scraped counting cats are unchanged across N consecutive ticks league-wide while games are in progress (would have fired ~23:30 UTC). Secondary/coincidental: an unrelated 55-min network outage killed 11 ticks (06:10–06:55, connect_tcp failures), so the last pre-settle tick was 06:05 not 06:55 — widens the visible step but no games were in progress then, so it hid nothing. NB wp_diff's gap warning did not fire on the 08:00–09:00 window (gap check appears to only consider ticks inside the window; the 07:00:26 tick fell outside) | yes — owner pushed on rate stats and was right: I had framed it as counting-cats-only; rate cats are stale too, via the _judge_group yardstick |
| 2026-08-06 | "did it happen again? bunch of WP shifts this morning" (wk18) | checked the new flag + the banked series before attributing | **Yes — recurrence, identical mechanism, and `INV_SCRAPE_STALE` caught it in production on its first night live.** League-wide H+R (period 18) stepped ONLY at the daily settles: 105 (8/04 20:00) → 293 (8/05 07:00) → 436 (8/06 07:00); frozen at 293 through the entire Wednesday slate. Flag fired **2026-08-05T19:10:08Z** (16 games in progress) and ran continuously to 04:35:16Z — 105 occurrences over two flag_dates — i.e. **11h50m of warning** before the 07:00 settle it predicted. This morning's shifts are that settle landing Wednesday's slate in one tick: all 6 matchups moved at 07:00:05, 5 of 6 the same direction, 35pp total absolute (WAR +10.5, Norsemen +9.4, Bear Nation +7.5, SwampD +3.7, Dawgs +3.6, Melonheads −0.3). Root cause unchanged and still live — re-probed the feed today: browser-UA request → 403, curl-UA → 200. NOT a new failure, no re-diagnosis needed. Flags left OPEN deliberately (they're accurate and the condition recurs nightly); they'll keep accumulating until the counting cats stop depending on the ESPN DOM scrape. Recommended remedy stands: extend the box-score reconstruction to H/R/HR/SB/K (data already fetched every tick — `live_batter_rows`), rather than fingerprint-chasing Akamai. **ORIGIN determined (owner asked "data source or app bug?"): DATA SOURCE, not ours.** Break window pinned to 2026-08-04 04:00:10Z (last live cat update, during Monday's slate) → 22:40Z (Tuesday's first pitch). Nothing local changed in it: `app/espn_scrape.py` last touched 2026-06-18, `pyproject.toml` 2026-06-02, Playwright 1.60.0 + Chromium bundle installed May 31 (untouched), `.playwright_profile` mtime May 31, macOS last updated Jun 25; the only commits in the window were Aug-3-morning docs/publish work, and the scrape then ran perfectly for ALL of Monday night (live H 23:00 8/03 → 04:00 8/04). Identical code worked at 04:00 and failed at 22:40 the same day. The failure is a **CDN block, not an app error**: Akamai "Access Denied" HTML with an `errors.edgesuite.net` reference, served at the edge. It is **host-wide on site.api.espn.com** (the generic `apis/site/v2/sports/baseball/mlb/scoreboard` 403s identically), not league- or feed-specific. Discriminator is client SHAPE, not identity — same URL same moment: browser-UA → 403, `python-httpx/0.28.1` → **200, 1.6 MB**. So our Python path is unaffected (`espn_public` probables overlay filled 28 today) and only the headless browser is blocked. *Hypothesis, undecidable from outside:* which signal — headless fingerprint, TLS JA3/JA4, the UA/client-hint mismatch (we claim `Chrome/148.0.0.0` while the browser is `HeadlessChrome/148.0.7778.96`), or IP/rate reputation from months of 5-min polling. Note curl WITH full Chrome headers passes while the real headless browser fails, which points past headers to an automation fingerprint. Consequence: **won't self-heal, and a workaround is an arms race** — but everything needed is reachable from plain Python. **FIXED 2026-08-06 (`729373d8`), and NOT by the box-score rewrite I first proposed — owner pushed back on the regression risk of swapping data sources and was right.** The 200-to-httpx finding was the answer sitting in plain sight: the block is on browser-shaped traffic only, so `espn_scrape` now re-fetches the blocked request server-side in a `page.route` handler and fulfills it into the page (`_serve_maybe_proxied`). ESPN still computes the scoreboard; we just stop letting one of its requests die. **Zero downstream change** — no new source, no schema, no attribution logic, nothing touched in sim/publish/front-end. Browser-attempt-first + proxy only on 403/dead (`should_proxy`) makes it self-heal if ESPN unblocks us; only the unauthenticated `site.api.espn.com` is routed (adding `lm-api-reads` would strip session cookies — guarded by a test). Verified: real scraper output identical to `category_state` for all 12 teams (9.7s), 358 tests, docs audit clean. **Still unproven until a live slate:** that the page renders LIVE values once the feed loads — `INV_SCRAPE_STALE` is the pass/fail signal and stays at `error`+unresolved until a clean slate proves it. Contingency (box-score reconstruction) fully scoped in the plan if it doesn't. LESSON: I over-scoped a fix past evidence I had already gathered; the owner's "isn't there a smaller path?" was the correction | **yes — owner rejected the first plan's scope and was right** |
| 2026-08-07 | verify the live-feed proxy fix on a live slate (wk18) | scheduled check; ran all 5 signals before calling it | **PASS — fix confirmed, flag resolved.** Slate opened 2026-08-06T18:55:02Z; checked 00:07Z, 5h12m in (detector needs active_start+65min to be meaningful). (1) `INV_SCRAPE_STALE` **did not fire once** — last occurrence 08-06T04:35Z, before the fix went live on the 09:55Z tick. (2) League-wide H+R climbed **459 → 524** across 37 distinct values in 63 ticks, vs frozen at 293 all night on 08-04/08-05. (3) `live_recon` **baseline verdicts 18 → 0** (now 21 `matched`, 1 `closer`, 2 `no_lines`) — the second-order damage (stale scrape poisoning the rate reconstruction via `_judge_group`'s yardstick) is gone too. (4) WP moved **11–27 times ≥1pp per matchup during the slate**, all six, vs flat until the settle on the broken nights. (5) 158 completed ticks, `live-scraped 120 category cells` every tick. One benign artifact noted: the 19:40:08Z tick wrote 7 of 24 cells (partial scrape) so a naive per-tick SUM dips — **zero actual team regressions**, the monotonicity guard held, and `check_scrape_staleness` requires a full per-series window so it stays quiet rather than false-firing on it. Flag resolved with provenance; check **retained** as the permanent regression detector (it is what will catch the next Akamai rule change). Box-score contingency not needed — remains scoped in the plan file if ever required | no |
| 2026-08-07 | look into the INV_SITE_QS_OVERCREDIT flag (1830 occurrences, only LIVE flag on the board) | evidence-first; two patterns spotted before any hypothesis — always **exactly +1**, always the **home** side | **FALSE POSITIVE in the check, not an over-credit on the site.** Concretely m105: site showed Swamp Dragons SVHD=1, `category_state` (scrape) held 0, so the check called 1 unsupportable. But `sim.load_settled_floor` returns **1** for that side — **Bryan Baker, 2026-08-04, sv=1**, pitcher slot 15, captured in the write-once `pitcher_final_lines` archive at Final and never banked by the idle scrape. The site was right the whole time. **Root cause:** publish applies the settled floor in TWO places — the fold's `max(scrape, floor+box)` *and*, independently, `cli._apply_qs_svhd_floor` (cli.py:1953) raising the display to `max(scrape, floor)` on every current-week publish (added 2026-07-27 for m96 QS 4→5). `validate._supported_credit` modelled only the fold and had an early `if not box: return scraped`, so with an empty unsettled window (slate over ⇒ box=0, verified: 0 pitcher lines) it never consulted the floor. Both observed patterns fall out of that: **+1** = one un-banked credit, not an accumulating double-count; **home-only** = coincidence of which side had floor > scrape (the check iterates both sides correctly). Also explains the publish artifact `SVHD=1` as an int among floats — the box/floor path, not category_state. **Fix:** always load the floor; ceiling is `max(scraped, floor + box)`. Guard NOT weakened — the deGrom shape (site 4 vs supportable 3, no floor) still fires. Regression tests both directions; the quiet-case test verified to FAIL with the fix stashed. 360 tests pass, docs audit clean, `check_published_site` against the live artifact now returns 0 findings. 11 flags resolved with provenance. **Lesson (added to the flag reference): this check is a second implementation of publish's rule — when publish gains a new path to a displayed value, the recompute must gain it too, or it becomes a false-positive generator that buries real flags** (it did: 1830 occurrences hid the board for 8 days) | no |
| 2026-08-09 | "any flags?" + clear the stale board (20 open across 5 codes) | checked `last_seen` and read the logs per code BEFORE resolving — did not blanket-dismiss | **All 20 resolved with per-code provenance; board now 0 open.** Two findings worth keeping. (1) **My "5 stale codes" summary was wrong** — `ANOM_SCRAPE_EMPTY` had instances 35h and 14h old and `ANOM_WP_SWING` one 5h old, so 2 of the 5 codes were live, not stale. Checking `last_seen` per code rather than eyeballing the list is what caught it. (2) **`ANOM_SCRAPE_EMPTY`'s message is misleading for its most common case**: both recent hits (08-07T22:45, 08-08T19:45, ×1 each) were **single-tick transients that self-recovered on the very next tick** — logs show `live-scraped 119 category cells` at 22:50 and 19:50 with healthy scrapes either side — yet the detail text always points at expired ESPN auth / re-running `espn_auth_setup.py`. A ×1 occurrence is a page-load hiccup (playbook #6); only a sustained count means auth. Worth rewording if it keeps costing a look. Other resolutions: `ANOM_CORRELATED_SWING` + the 08-05T07:01 `ANOM_WP_SWING` cluster = the broken-scrape night's settle (fixed by the live-feed proxy, verified 08-06); `ANOM_WP_FLAPPING` m98/m99 = true positives on the same stale-scrape period; week-17 `ANOM_WP_SWING` m98 −78.1pp = the settle cliff already investigated 08-03. **Left flagged as known-incomplete:** `INV_EMPTY_BUDGETS` (m98 ×37, m100 ×5, 08-02/08-03) recurred **after** commit `f31ace2d` (2026-07-29) which was meant to fix exactly this end-of-week-drain false positive — so `_side_remaining` and `build_budgets` still disagree in some end-of-week state. Resolved as benign (winners correct, no fetch actually failed) but explicitly noted as a candidate for another look if it fires at the end of week 18 | no |
| 2026-08-10 | morning check after wk18's final slate: did the CLOSING SCRAPE work? | evidence-first; separated "the gate fired" from "a credit actually moved" instead of calling PASS on the first signal | **MECHANISM PASS, OUTCOME NOT EXERCISED — Phase 2 is NOT yet unblocked.** Slate 16:15:05Z→02:40:14Z, a later Final at 04:05:14Z. The gate fired exactly as designed, **twice**, each time 4 ticks (~20 min) then stopping: scraped at 02:40/02:45/02:50/02:55 with `in_progress=0` then skipped from 03:00; scraped again 04:05/04:10/04:15/04:20 then skipped from 04:25. It did **not** run a headless browser all night (silent 03:00–04:00 and 04:25 onward) — the throttle works. **BUT the outcome signal had nothing to prove:** of the 21 Final pitcher lines from games finalizing after the last credit (23:00:32Z), **zero** were rostered+slotted, so no QS/SVHD credit was pending for the closing scrape to bank. Nothing appeared at the 07:00 settle either, which is *consistent* with the fix but is not proof. The untested link remains: **does ESPN post a credit promptly when the game that earned it is the last one running?** Needs another slate where a rostered pitcher earns a late QS/SVHD. Do not delete the floor on this evidence. **NEW DEFECT — `INV_SCRAPE_STALE` false-positived twice** (23:20–23:26 ×2, 01:30–02:35 ×6), my own check from 2026-08-05. With only **2 games in progress** league-wide H+R legitimately stalls: observed 1056→1057 (95 min), 1059→1060 (55 min). The check demands movement within 9 ticks (45 min) and gates only on `in_progress > 0` **without scaling for how many games are live** — impossible to stall that long on a full slate, routine on a 2-game tail. Scrape was verifiably healthy throughout (119 cells every tick; credits banked live 20:10–23:00). Fix: scale the window by in-progress game count, or require a minimum live-game count. **`INV_EMPTY_BUDGETS` RECURRED** (m104 ×32+×5, m106 ×32+×5) — third occurrence after `f31ace2d`; left unresolved per instruction, `_side_remaining` still disagrees with `build_budgets` at end-of-week. **Week 18 all 6 decided, two upsets vs Sunday-noon WP:** Jo Mamas won m105 from 15.1%, That Bus won m106 from 21.3%. **Playoff live-finale refresh worked:** 20 runs during the slate vs the usual ~3 in that span (one gap 23:06→00:25, 79 min, worth a look). Health: site fresh (5.4 min), all recent deploys `success`, no stuck run; 9 of 137 overnight ticks started-but-never-done (~7%, likely the ustats CPU contention); one `ANOM_SCRAPE_EMPTY` at 20:20:51 that recovered on the very next tick | no |
| 2026-08-10 | wk18 m105: Details shows "Mamas 100% SVHD at 4-3" while the scoreboard on the same page shows SVHD tied 4-4 | **WRONG — "the scrape missed Baker's 8/04 save; the floor's 4 is right; the sim short-circuits past `load_settled_floor` ([sim.py:2389](app/sim.py:2389)) so Details is stale."** I inverted truth and bug: I treated the archive-derived floor as authoritative and never asked ESPN. | **The scoreboard's 4 is the phantom; Details' 3 was right all along.** ESPN REST (`espn.fetch_all_matchups`, m105) returns period 18 home SVHD **3.0 LOSS** / away 4.0 WIN — the scrape was never wrong. The floor over-counts because `load_settled_floor` gates credits on **our** `daily_lineups` snapshot, and the 08-04 snapshot (taken 22:40:04Z, *before* ESPN's lineup locked) recorded **Bryan Baker slot 15**; ESPN's historical `mRoster` for spid 133 has him **slot 16 = bench**, so ESPN correctly withheld the save. "First snapshot per day wins" then froze the pre-lock slot forever. **This explains 100% of the floor's disagreement with the scrape across the whole season** — over weeks 1..18, floor > scrape in only **3 of 432** settled (matchup,team,stat) pairs, and each is exactly one credit whose slot we recorded active and ESPN recorded 16: m98 t11 QS (Wacha 07-31, ours 14 / ESPN 16, floor 3 vs scrape 2), m101 t7 SVHD (Abreu 07-29, ours 13 / ESPN 16, 6 vs 5), m105 t3 SVHD (Baker 08-04, 4 vs 3). Drop the mismatched credit and floor == scrape in all three. **Corollary — the 2026-08-07 `INV_SITE_QS_OVERCREDIT` weakening was built on this same misdiagnosis:** CLAUDE.md records "the floor genuinely held Bryan Baker's 8/04 save" as the reason those flags were false positives. It did not; the check was correctly reporting a phantom credit, and teaching `_supported_credit` to consult the floor silenced a **true** positive. The "1830 false positives" were ~3 distinct real phantoms re-counted every tick (m105 alone: occurrences 185 + 233 + 117). **Owner's premise was also inverted** — the ask was "fix it before the rollover drops the scoreboard to 3", but 3 is ESPN's number, so the rollover *corrects* the display rather than breaking it; did not implement the requested change. Real defect to fix is the pre-lock `daily_lineups` snapshot (ESPN's per-scoringPeriod `mRoster` is authoritative and `espn.fetch_daily_lineups(scoring_period_id)` already fetches it). **FIXED same day** on owner's go-ahead: `refresh-live` now pulls each game-day's lineup by its own `scoringPeriodId` and *replaces* the stored rows (`cli._authoritative_lineups`/`_replace_daily_lineups`, `mlb.scoring_period_for_date`); `app backfill-lineups` repaired 14 days, correcting **43 slots across 8 of 12 teams** — mostly in pairs (one bat out, one in), i.e. ordinary late swaps, so the pre-lock capture was systematic rather than a one-off. Floor now equals ESPN's settled scrape on **432/432** pairs (was 429/432). `publish --rebuild` corrected the site to SVHD 3-4 LOSS. Tests: `tests/test_daily_lineups.py`. | yes — owner's pushback premise was itself wrong, but chasing it is what surfaced my inverted attribution; I should have queried ESPN before trusting the archive |
| 2026-08-10 | wk19 Norsemen RP appearance projections implausible (Vesia 5.88, Morejon 5.75, Soto 6.50 apps; K proj ~6.4-6.8 vs season-actual 3-4 K/matchup) | code-first, no pre-tool claim | **Denominator-span mismatch in the RP appearance share ([sim.py:1690](app/sim.py:1690)): `units = (gp_ros/total_ros) × rp_remaining`, but `total_ros` = `load_total_remaining_games(current, last_reg=22)` counts team games only through the last fantasy regular-season week, while ESPN's ROS `gp` projection spans the whole remaining MLB season (through period 25, Sep 27).** Numerator covers ~43 games, denominator ~24 → share inflated ×(games thru 25 / games thru 22). Smoking gun: Soto share = 26/24 = 1.08 > 1.0 → 6.50 projected appearances in a 6-game week (more apps than team games, impossible). Inflation grows hyperbolically as wk22 nears (LAD: wk10 ×1.23, wk15 ×1.37, wk19 ×1.76, wk20 ×2.06, wk21 ×2.58, wk22 ×4.17). Historical start-of-week snapshots (wks 11-18, `details_json` budgets vs `pitcher_final_lines` actuals): Vesia proj 29.4 apps vs 23 actual (×1.28), Morejon ×1.44, Soto ×1.78 — over-projection tracks the growing truncation, and ESPN's underlying per-game appearance rate itself checks out (Vesia actual ~0.44 apps/team-game ≈ ESPN's 21/44 = 0.48). Same `total_ros` also feeds the future-week SP flat share ([sim.py:1641](app/sim.py:1641)) and `_sp_relief_svhd` ([sim.py:1757](app/sim.py:1757)) — same inflation there. Fixed same day (owner-approved): `load_total_remaining_games` now defaults to unbounded (through the stored schedule's end, skipping Postponed/Suspended/Cancelled rows), both cli call sites drop the bound, and the RP branch gained a physical ≤1-appearance-per-team-game backstop (`rp-apps-capped` flag). Corrected wk19: Vesia 3.34 / Morejon 3.21 / Soto 3.71 apps. Audit for siblings: hitters immune (per-game ROS rates cancel; units from the actual schedule), QS/per-out SP rates immune (rates cancel), playoffs loader already full-span, ratio-v0 has no schedule math. Residual (hypothesis, unverified): intra-week drift — the denominator counts the whole current week incl. already-played games while ESPN's ROS numerator shrinks daily; ≤~7% mid-week now, grows late September. Tests: tests/test_total_ros_games.py | no |
| 2026-08-10 | how accurate are start-of-week category projections, historically? (follow-up to the RP denominator fix) | design-first: chose continuous per-category projected-vs-actual over WP calibration because only 54 settled matchups have a pre-play snapshot (snapshots begin 05-28) — binary WP calibration has almost no power at that n | **EVERY counting category is over-projected at start of week, monotonically one-directional — new re-runnable tool `scripts/calibration.py`.** Forecast = last tick before the period's first pitch (verified: 5 min pre-first-pitch, probables in, cadence on, zero banked — the model at its *most* informed). Periods 10-18, 108 team-week observations per cat, 90% CI bootstrapped clustered by week: **H +8.3% [+5.9,+10.8] · R +7.1% [+2.4,+12.2] · HR +2.5% [-5.7,+11.7] · SB +19.6% [+10.2,+30.0] · K +18.4% [+14.5,+22.4] · QS +40.5% [+32.7,+48.2] · SVHD +55.1% [+45.1,+65.1]**. (1) **The hitter side decomposes exactly.** Unit-free ratio test (cancels lineup-days): R/H −1.0%, HR/H −5.1%, SB/H +10.5%. So a **shared ~+8% units (lineup-days) over-projection** × per-cat rate error reproduces every hitter total to within 0.5pp (1.083×0.990=+7.2% vs R's +7.1%; 1.083×0.949=+2.8% vs HR's +2.5%; 1.083×1.105=+19.7% vs SB's +19.6%). Hypothesis for the units bias: `_hitter_days_slotted` runs an *optimal* bipartite matching and ignores the manager's bench, i.e. assumes daily lineup optimization real managers don't do. (2) **CLAUDE.md's "most hitters' HR rates (projection ~2× actual)" caveat is WRONG as it flows through to scored totals** — the HR *rate* is slightly UNDER-projected (−5.1% vs H); HR's small +2.5% total is the units bias partly cancelled. Corrected in the docs. (3) **QS +40.5% CONFIRMS the doc's other untested claim** (ESPN ROS QS rate inflated — the Skenes 86%-vs-45% note), and its trend is *negative* (−0.025/wk, +53%→+24% over wks 10→18), i.e. improving, consistent with the promoted-starter/QS-rate work landing late June. (4) **K's bias is NOT mainly the RP denominator bug** — trend is flat (+0.006/wk) where a span bug grows; SVHD's *is* growing (+0.033/wk, +32%→+72%), matching the denominator's ×1.29→×1.51 growth over that span. So today's fix should cut SVHD (and part of K) going forward but leaves QS and the hitter-units bias untouched — those are live. (5) **Why this never showed up in WP:** the bias is one-directional across *both* sides, so it largely cancels in a head-to-head category comparison — exactly why fixing the ×1.76 RP inflation moved week-19 WPs ≤2pp. It bites only via roster asymmetry and via the rate cats (over-projected innings move ERA/WHIP denominators non-linearly). (6) Part of the pitching bias is **deliberate**: BE-slot pitchers are projected (streaming hedge) and IL-slotted players with return dates are projected (owner decision 2026-07-22, explicitly accepting over-credit) — but +40%/+55% likely exceeds what those choices intended. All mechanism attributions above are **hypotheses**; the measurement is solid, the causes need verification. Rate cats excluded (their stored `avg` is an internal scale). Cannot be re-simmed (`player_projections` has no period key) | no |
| 2026-08-10 | fix the QS over-projection (+40.5%) by blending ESPN's ROS rate toward season-to-date actuals | measured the level bias directly before writing code, and derived the shrinkage weight rather than picking one | **Implemented as empirical-Bayes shrinkage at fetch time** (`espn.blend_qs_rate` / `apply_qs_rate_blend`, `QS_RATE_PRIOR_STARTS = 9.0`): `rate = (act_qs + K·prior)/(act_gs + K)` with `prior = ros_qs/ros_gs`. **Confirmed the level bias independently of the historical analysis:** across 65 rostered starters / 1161 actual starts, ESPN-implied QS rate **.598 vs actual .438 (+36.7%)** — a second route to the +40.5% total bias `calibration.py` found, which cross-validates both. **K is measured, not chosen** (Beta-Binomial method of moments: var(observed) .0370 − binomial noise .0128 = between-pitcher spread .0242 → K = 9.0 pseudo-starts ⇒ ~70% weight on actuals at a typical 21 starts; new re-runnable `scripts/analyze_qs_rate.py`). Chose shrinkage over SVHD's hard 15-GP cliff precisely so low-sample pitchers degrade gracefully; chose ESPN's rate as the prior (not the league mean) to keep its per-player *ranking* signal, accepting a residual level bias rather than coupling every projection to roster composition via recentering. Written back as a **total** (`rate × ros_gs`) so `sim` needed **zero changes** — it recovers the rate as `ros_qs/gs_ros`. **Verified live, not just in tests:** refresh-rosters at 13:03:05Z, then recomputed wk19 + wks20-22. League projected QS **−14% in every week** (wk19 54.4→46.9, wk20 57.8→49.9, wk21 58.9→50.6, wk22 63.4→54.2); **it is a blend, not a haircut** — Logan Webb rose .636→.658 (actual .667) while Peralta fell .600→.294 (actual .174), Jax .700→.307 (actual .111). Max |ΔWP| 2.7pp, consistent with the now-established pattern that a symmetric league-wide bias mostly cancels head-to-head. **Honest residual:** expected QS bias after this is **≈+20%, not the +11.8% the rate-level number suggests** — the +40.5% total decomposes as rate bias × *start-count* bias and only the rate half is fixed; the remainder is over-projected starts, the same suspected root as K's flat +18% (whose trend ruled out the RP denominator). Next lever is start counts, not QS. 401 tests pass (`tests/test_qs_rate_blend.py`: shrinkage formula, monotone sample-size weighting, tiny-sample stays near prior, decline-when-nothing-to-blend, [0,1] clamp, total-not-rate write-back, reliever no-op, string coercion) | no |
| 2026-08-10 | "fix the start count over-projection" (the residual after the QS rate blend) | measured the decomposition BEFORE proposing a fix, splitting projected starts into "never started" vs "started but not credited" | **NO GENERAL BUG TO FIX — the +23.7% splits ~50/50 into one deliberate choice and one once-a-season artifact.** New re-runnable `scripts/analyze_starts.py` (periods 11-18, 96 team-weeks): projected SP starts 769.3 vs 696 actually **made** (+10.5%) vs 622 **credited** in an active pitching slot (+23.7% — and credited is the accounting that matches ESPN's banked totals, so it's the one `calibration.py`'s biases should be read against). (1) **Slot attribution = 74 of 696 real starts (10.6%)** made by pitchers who were bench/IL-slotted that day, so they scored for nobody. I measured the activation rates (prior slot active → 97.8% 453/463; bench → 74.9% 125/167; **IL → 8.3% 1/12**) and implemented an activation-probability discount, then **the owner reverted it by decision**: keep assuming managers activate benched starters, model everyone as competent, never penalise a team's WP for a neglectful owner. Explicitly chose "no discount at all" over "discount IL only". Reverted cleanly (`git checkout app/sim.py`); constants + rates recorded in CLAUDE.md so a future session doesn't silently re-add it. (2) **The rest is the All-Star fortnight**: rotation error is **+3.9% (90% CI [+1.7,+6.3]) in normal weeks** — irreducible rotation churn — but **+43.9% in period 15**. Mechanism confirmed from the schedule, not assumed: a `LONG_MATCHUPS` period has 14 calendar days but only **11 game dates, 0.68 games/team/day vs ~0.90** normally, and both rotation models (the cadence rest-day walk and the flat `MAX_SP_RATE × open_weight` share) walk straight through those ~3 gameless days → 2.26 projected turns/starter vs 1.57 actual (deGrom 2.98→1, Misiorowski 3.00→1, Max Meyer 2.98→1). Left unfixed on purpose: only period 15 is long, so it costs nothing for the rest of 2026 and can't be validated again until July 2027; logged in known limitations. (3) **This closes the pitching-bias accounting.** Against credited starts, K's +18.4% total ⇒ per-start K rate **−4%** (ESPN's K rate is fine, contrary to my earlier "K bias is units-driven" hypothesis — it is, but the units half is the deliberate slot choice); QS's +40.5% ⇒ per-start QS rate **+13.6%**, exactly what the same-day QS blend removed (−14%). So the residual pitching over-projection is now essentially all the deliberate activation assumption — as calibrated as the modelling choices allow. **Correction to my own earlier claim:** I had told the owner "the next lever is start counts, not QS"; the measurement shows the *fixable* part of start counts is ~+3.9%, and the big number was a policy choice plus a calendar artifact | no |
| 2026-08-10 | apply the QS empirical-Bayes shrinkage to the SVHD rate (replace the hard 15-GP cliff) | re-measured the calibration baseline first, then measured K rather than porting the QS constant | **Implemented** (`espn.blend_svhd_rate` / `apply_svhd_rate_blend`, `SVHD_RATE_PRIOR_APPEARANCES = 8.0`, `MIN_ACT_GP_FOR_SVHD_RATE` deleted): `rate = (act_svhd + K·prior)/(act_gp + K)`, `prior = proj_s83/proj_gp` (the **full-season** projection — ESPN's ROS 83 is encoding-broken so it can't be the prior the way `ros_qs/ros_gs` is for QS), written back as a total `rate × ros_gp` so `sim` needed zero changes. **(1) Re-ran `calibration.py` first, as instructed — and the honest finding is that it CANNOT show the denominator fix**: it scores the model as it ran and the weeks can't be re-simmed, so it still printed the identical +55.1%. Estimated the residual instead by dividing each week's projection by that week's measured RP-denominator inflation `G(w..25)/G(w..22)` (1.218 wk10 → 1.585 wk18): **+55.1% → est. +15.2%, and the growing trend vanishes** (residuals 32/18/6/4/7/36*/11/11/8%). A lower bound — it divides the *whole* projection, so where `rp-apps-capped` bound, the true figure is higher. Conclusion: **the denominator fix was ~three-quarters of the SVHD problem; the rate is the small half.** **(2) Found two defects in the sibling `analyze_qs_rate.py` while mirroring it, neither fixed** (changing QS's K moves live projections and needs its own verification; logged in CLAUDE.md + a spawned task): it fetches with `{"scoringPeriodId": 0}`, which makes ESPN **omit the split=6 ROS block for ~60 rostered players** (a third of the reliever pool: Bryan Baker, Gregory Soto, Alex Vesia, Kevin Kelly…), so K was measured on a biased subset; and its spread-based estimator assumes the prior is the population mean. **(3) The estimator matters more than the constant.** Spread-based MoM says K≈14; but the objective is the blended rate's error against a *per-player* prior, giving `K = p(1−p)/E[(prior−true)²]` = **7.6**, and a direct squared-error sweep independently optimises at **6**. The gap is entirely ESPN's preseason prior being *structurally* wrong for mid-season role-changers — **5 of 47 rostered relievers carry a .000 projected rate against realized rates up to .571** (Jacob Latz, Yoendrys Gómez, Ian Seymour, Grant Taylor, Sean Manaea); drop those five and spread-MoM and prior-error MoM reconcile (12.9 vs 14.3). Picked **8.0**, rounding toward more shrinkage because a reliever's realized rate is itself selection-inflated (he is rostered *because* he started earning saves), which makes the back-test overstate how wrong the prior is. Error curve flat K=4..10. **(4) Verified end-to-end, not just in tests**, by A/B-ing both arms against a `VACUUM INTO` sandbox copy of data.db (production untouched) from one ESPN payload, changing only stat 83: **league projected SVHD −2.2% for wk19, max |ΔWP| 3.1pp**, and a genuine blend — Bryan Abreu *up* (.311 realized / .620 prior ⇒ .358), Bryan Baker down (.750 / .294 ⇒ .685), Edwin Díaz 7.97→6.44. The back-test says the blend beats the cliff at **every** sample size (−38% squared error over n=1..60, −46% over n=1..25) but the live effect is small **because by mid-August every rostered reliever is already past the old 15-GP threshold** — 22 players sat below it and 20 of them were starters projecting 0.00 SVHD anyway. **This change's value is early-season and at the boundary, not today.** **(5) The A/B caught a real bug in my own port that unit tests did not**: ESPN **omits stat 83 entirely when it is zero** (83 of 133 rostered pitchers with appearances; an explicit `0` never occurs), and I had carried over the QS guard treating a missing value as *unknown* → every save-less arm fell back to ESPN's prior. Kyle Leahy, **0-for-22**, jumped from a projected 0.00 to 3.91 ROS SVHD. Fixed (missing + appearances ⇒ zero) with a named regression test; QS is unaffected — stat 63 is always present for a pitcher with starts. Lesson: the end-to-end A/B, not the unit tests, is what surfaced it. **Honest scorecard:** aggregate SVHD improves only ~2pp of an estimated ~15% residual, and the residual is now mostly **appearance counts**, not the rate — the same units story as the hitter cats. 421 tests pass (`tests/test_svhd_rate_blend.py`, incl. the no-discontinuity-at-15 regression and the missing-means-zero case) | no |
| 2026-08-10 | Re-measure `QS_RATE_PRIOR_STARTS` (the two defects logged against `analyze_qs_rate.py`) | Going in, the expectation on record (CLAUDE.md's note + the task framing) was that **both defects push K down**, i.e. toward trusting actuals sooner, so 9.0 was too high | **Both defects are real but push in OPPOSITE directions and very nearly cancel — 9.0 → 7.0, a second-order tweak, not a correction of a broken number.** Fixing the sample pushes K *up* (spread estimator 9.0 → 15.5); switching estimators pushes it back *down* (18.3 → 8.6 on the correct sample). **(1) Defect 1's mechanism was mis-diagnosed and is now corrected in the docs.** `{"scoringPeriodId": 0}` does *not* make ESPN omit the split=6 ROS block — for a player present in *both* responses the block is byte-identical. It returns a **different, stale roster snapshot**: 108 of its 279 players are not on any current roster, and it misses **41 of the 90** currently-rostered usable starters (deGrom, Wheeler, Cole, Snell…). Verified against the league's own stored rosters — plain fetch 283/283, `scoringPeriodId=0` only 206/279, and an explicit current `scoringPeriodId=139` reproduces the plain fetch exactly. So the sample was biased, just not for the stated reason. **(2) The stale sample also overstated ESPN's QS level bias**, which was being quoted in three places: **.598 vs .438 (+36.7%) over 65 starters → .596 vs .467 (+27.5%) over 89** (1714 starts). Corrected in `blend_qs_rate`'s docstring, CLAUDE.md and the test module docstring. **(3) Picked K by the back-test, not the estimator.** Prior-error MoM says **8.6**; the ported squared-error back-test optimises at **7.0** and does so under *three* weightings — uniform n=1..34, today's per-pitcher start counts, and ROS-starts-remaining — so it is not an artifact of weighting low-n heavily. Took 7.0: it measures the objective rather than approximating it, and selection makes both numbers *upper* bounds (a rostered starter's realized rate is luck-inflated above true talent while ESPN's prior sits above the realized rate ⇒ true prior error larger, true K smaller). Note this rounds the opposite way from SVHD's 8.0, where the prior's error ran the other direction. Curve flat K=6..10 (within 2.4%); K=9.0 was only **+1.2%** off optimum, which is why the live effect is tiny. **(4) Verified end-to-end, same protocol as the SVHD change**: `VACUUM INTO` sandbox copy, `db.DB_PATH` repointed, production `data.db` untouched, one cached ESPN payload feeding both arms, only stat 63 rewritten, paired RNG seed, real `cli.compute` per arm. Result: wk19 league projected QS **46.9 → 46.3 (−1.3%)**, **max |ΔWP| 0.2pp** over all six matchups (88 of 90 stat-63 rows moved, so the small WP effect is genuine cancellation, not a no-op). Sanity check that the harness was faithful: the K=9 arm reproduced CLAUDE.md's documented wk19 46.9 exactly. **(5) Also fixed `--min-starts` default 1 → 3**, matching the SVHD script — a 1-start pitcher contributes a maximal squared deviation with a *zero* binomial-noise correction, biasing K down with no information (7.2 at min 1 vs 8.6 at min 3). **Lesson:** two defects in the same estimator are not additive, and a note asserting a *direction* ("both push K down") is a hypothesis, not a finding — the decomposition matrix (sample × estimator × min-starts) took one run and refuted it. 434 tests pass; the sanity bound on K is now the measured flat basin (6..10) rather than an eyeballed 3..25 | no |
| 2026-08-10 | make the calibration measurement a recurring check | designed the *detection rule* before writing it, then replayed it against real history rather than trusting thresholds | **`ANOM_CALIBRATION_JUMP` (warn), daily tier — the battery's first and only check that compares a projection to a realized outcome.** Motivation, evidenced: I queried every code that has ever fired and all 14 are change detectors, freshness detectors, or internal invariants; none asks "was it right". Confirms both of today's bugs were **flag-invisible by construction** (RP inflation ran all season growing to ×1.76; QS rate +28% — zero flags ever). Design decisions, each with a reason: (1) **daily, not per-tick** (`run(..., calibration=True)`, wired from `daily.sh`, non-fatal) — retrospective + aggregate, reads `details_json` across every settled week, and the answer only moves when a week settles; per-tick would inflate `occurrences` into meaninglessness. (2) **Fires on a jump, never on the level** — the level bakes in the deliberate bench/IL-at-full-weight choice (~10.6% of starts), so a level threshold either fires forever (the `INV_SITE_QS_OVERCREDIT` 1830-false-positive mode) or is too loose to catch anything. (3) **Jump scaled by each category's own volatility (robust MAD)** because noise floors differ wildly — H moved in a ~5pp band, HR ranged −17%..+41%; one flat tolerance cannot serve both. (4) **Replay-verified, not asserted** (the `INV_SCRAPE_STALE` discipline): first pass produced 3 false positives — wk15 K and wk15 SVHD (fixed by excluding `LONG_MATCHUPS`: a fortnight isn't comparable to a week and carries the known +44% rotation artifact) and wk16 K at +12.5pp with a collapsed MAD scale (fixed by raising `CALIBRATION_MIN_ABS` 0.10→0.15; K's prior weeks 11,12,11 give MAD 1pp ⇒ 3σ≈4pp). Final: **zero false positives across periods 10-18**, sensitivity at wk18 e.g. K fires outside [−10%,+45%], QS outside [+6%,+73%]. (5) Detail carries the window **trend** as the mechanism hint (growing ⇒ span/denominator, flat ⇒ rate) — the signal that actually identified the RP bug. (6) Extracted the measurement to **`app/calibration.py`, shared by the report and the check**, specifically to avoid the second-implementation drift that turned `INV_SITE_QS_OVERCREDIT` into a false-positive generator. **Documented limitation:** catches step changes, not slow drift (the RP bug crept ~+5pp/wk, inseparable from noise at this n) — slow drift stays the human read of `scripts/calibration.py`. 448 tests pass (`tests/test_calibration_check.py`: quiet on a large-but-stable bias / on ordinary wobble / on a noisy low-event cat, fires up and down, one finding lists all offenders, off the per-tick path, long periods excluded, floor guard). **Note:** the concurrent SVHD task found two real defects in my earlier QS work — `analyze_qs_rate.py` fetched with `scoringPeriodId=0` (a stale roster snapshot, 108/279 players not currently rostered, missing 41 of 90 starters) and used the between-player-spread estimator, valid only when shrinking toward a population mean rather than a per-player prior. Its corrections (K 9.0→7.0, level bias +37%→+28%) are right and I left them in place | no |
| 2026-08-10 | tail calibration: when the model gives a side <5% to win a stat category, does it win more often than that? | Measured the whole reliability curve before proposing anything. The standing prior on record was CLAUDE.md's "if a category WP feels too lopsided, it's almost always projection asymmetry, not variance -- `_decide` is doing the right math", i.e. extreme category probabilities are benign by default | **The low tail is real and ~2.8x overconfident, and the standing note is half right in a way that matters: the math given the projections is fine, but the projections carry several units of error that never enters the distribution.** Sample: periods 10-18, 54 settled matchups, all 10 cats, every in-week non-`edited` snapshot = 1,872,840 forecasts over 1080 (matchup x side x cat) units; ground truth derived from ONE home-vs-away comparison per cat (never the independently-stamped per-team `result`, per the 2026-06-06 asymmetric-records lesson). **(1) The curve.** p<5%: 274,258 forecasts, predicted 1.26%, observed **3.49%**, ratio **2.77, 90% CI [1.55, 4.17]** (cluster bootstrap by matchup, 54 clusters -- clears 1.0). By band: <0.1% -> 84x, 0.1-0.5% -> 8.5x, 0.5-1% -> 2.7x, 1-2% -> 3.8x, 2-5% -> 1.9x. The **body is well calibrated** (5-10% -> 1.01x, 10-20% -> 1.06x, 30-50% -> ~1.0x), so this is a tail defect, not a global miscalibration. Same fact from the user-facing side: **cats priced >95% are lost 3.4% of the time vs the 1.3% advertised**. Concentrated in four cats -- **SVHD 9.7x, QS 5.0x, ERA 4.0x, SB 2.2x**; K/OPS ~1.1x; H/R/HR/WHIP <=0.3x (fine). **Honest caveats stated up front:** snapshot-weighted rates over-represent long-lived tail states (top 10 units = 84% of all tail wins) and the 84x bin is a handful of episodes; the unit-level restatement is **576 of 1080 units dipped under 5% at some point and 38 won the cat anyway**. **(2) Mechanism 1 -- the mean is wrong, not the spread (32 of the 38 comebacks).** At the last snapshot where the side was still under 5%, the *projected margin already had the wrong sign*. Mean projection error over the comeback cases: favourite **+3.4 SVHD / +8.1 K / +4.1 R / +3.0 SB / +1.0 QS**, comeback side under-projected. This is today's documented level bias (SVHD +55%, QS +40%, K +18%) -- which cancels head-to-head only when both teams have similar *remaining volume*; under roster asymmetry / off-days / two-start weeks the residual is a systematic shift in the **margin**, and the sim treats the shifted margin as certain. Worst case m89 SVHD: projected 14.6 v 6.9, final **8 v 9**. **(3) Mechanism 2 -- no parameter uncertainty.** The sim samples player-level outcome noise around fixed projected rates and never resamples the rates. Backing the implied sigma out of (p, projected margin) as `margin/probit(p)`, one observation per unit at Tue/Thu/Sat checkpoints, 0.05<p<0.95: realized/simulated spread = **SVHD 1.55, K 1.47, QS 1.38**, everything else 0.85-1.15. The three under-dispersed cats are exactly the three that dominate the tail. **(4) Mechanism 3 -- hard structural zeros; the outcome is not in the sim's support.** 61,446 forecasts (3.3%) were a literal 0/10000; 487 of them won, across 6 units. Traced two end-to-end: **m103 QS 2026-08-09** -- banked 2, opponent locked at 3, one SP left in the budget, so max reachable = 3, `p_win` exactly 0 with `p_tie` 0.55; away finished **4** because Sean Manaea threw a QS from active slot 14 and **first appears in ANY period-18 budget at 18:05, i.e. at his own first pitch** (a Sunday streaming add). **m70 QS 2026-06-21** -- budget held five RPs and zero starters; Davis Martin started from slot 14 and threw a QS, and Brandon Young (budgeted `role=RP, exp_qs=0`) also started (GS=1). The sim conditions on the current roster and current probables as if fixed for the rest of the week; measured churn says they are not -- **66 of 108 team-weeks (61%) gain at least one player into the budget after first pitch, 1.3 entrants per team-week**. Related gap: `_open_sp_game_weight` (app/sim.py:865) drops any game with an announced probable from the cadence pool, so a probable we fail to attribute to a rostered arm loses that start from the fixed **and** the stochastic piece. **(5) Proposed, NOT implemented, and not yet owner-reviewed:** (a) a **floor on published per-category probabilities while games remain** (~0.5-1pp, renormalised) -- mechanism 3 cannot be fixed by better projections, and a model that has never observed roster churn should not be allowed to print 0.0%; (b) a projection-error term on the SVHD/QS/K rates so the margin distribution reflects the error the model demonstrably makes. Sizing (a) against the observed hazard rather than picking a round number is the next measurement. **Scope note:** first pass was throwaway scratchpad scripts; now productionised as `app/tail_calibration.py` (measurement) + `scripts/tail_calibration.py` (report) + `tests/test_tail_calibration.py` (26 tests), so this IS re-measurable after a fix. **Incidental find, since resolved:** mid-investigation the working tree had `app/calibration.py` untracked while the committed `app/validate.py` imported it (`app validate` would have failed on a fresh clone). It turned out to be a local lag, not a real gap -- `d759ef7a8` had both files and reached this checkout via a cron merge while the analysis was running. Worth recording only as a reminder that on this repo the working tree moves under you every 5 minutes | no -- measurement-first, but the conclusions and the two proposed fixes are unreviewed |
| 2026-08-10 | follow-up: strip out everything roster churn can explain -- is the tail STILL overconfident, i.e. is there a systemic skew in how a stat's WP is calculated? | Owner's call, and it is the right one: "projections are based on current rosters and don't include roster changes" is a documentable limitation, not a bug. So the real question is what survives that exclusion. Classified a forecast as churn-exposed if the side later gained a budget entrant of the relevant type (HIT for batting cats, SP/RP for pitching), built from every snapshot's `*_budgets` first-appearance timestamp -- a deliberately GENEROUS filter (it excuses the forecast whether or not the newcomer actually produced), so the residual is a lower bound on the model's own error | **Yes -- churn is a minority of it. It explains 11 of the 38 comebacks and 20% of all forecasts, and the <5% band only moves from 2.77x to 2.57x [1.18, 4.19]. The residual localises hard onto the three pitching COUNTING cats, and it is two compounding calibration defects, neither roster-related.** **(1) Where it survives** (<5% band, churn-exposed forecasts removed, cluster-bootstrapped by matchup): **SVHD 8.3x, QS 5.1x, ERA 4.9x, SB 2.5x**; H 0.00, R 0.07, HR 0.27, WHIP 0.48, OPS 1.08 -- all fine. **K collapses 1.08 -> 0.38: K's entire tail problem WAS churn** (m57/m103 streamed starters), and it is the one cat the filter fully exonerates. Caveat stated rather than buried: ERA's 4.9x rests on 2 episodes (m55, m72) and SB's 2.5x on 1 (m63) -- wide CIs, do not treat those two as established the way SVHD/QS are. **(2) Defect A -- the model over-differentiates teams.** Regressing the settled margin on the projected margin through the origin (one obs per matchup x cat at Tue/Thu/Sat checkpoints, churn-exposed sides dropped): **K 0.63 [0.55, 0.71], QS 0.57 [0.36, 0.76], SVHD 0.58 [0.47, 0.71]** -- all three CIs exclude 1.0. **The projected gap on the pitching counters is routinely ~40% bigger than the one that materialises.** Every hitting cat sits at 0.81-0.92 with a CI containing 1.0 (H 0.81, R 0.87, HR 0.92, SB 0.92, OPS 0.91), and WHIP 0.94. This is the same multiplicative level bias already on the books, seen from the margin side rather than the level side: it inflates the *gap* by the same factor it inflates the *total*, and the sim then prices the inflated gap as solid. **(3) Why pitching and not hitting -- the cancellation argument has a volume precondition.** A shared multiplicative bias cancels head-to-head only when both teams carry similar volume. Measured relative gap (mean |projected margin| as a share of a team's projected total): **H 10%, R 9%, HR 17%, K 19%, QS 20%, SB 35%, SVHD 42%**. Every team plays 6-7 games so lineup-days are near-uniform and the hitter bias genuinely cancels; bullpen usage and rotation turns are not uniform at all, so on SVHD the same bias scales a gap that is already 42% of the total. **This is the missing precondition in the standing "it largely cancels head-to-head" note** -- true for the level, false for the margin whenever volume is asymmetric. **(4) Defect B -- the within-week distribution has a heavy TAIL, not a wide middle. CORRECTED while building the script: my first pass reported "realized/simulated spread SVHD 1.55, K 1.47, QS 1.38" from `pstdev(error)/median(sigma)` on churn-unfiltered data, and that does not survive a robust estimator.** Re-measured as standardized residuals z = (actual - projected margin)/sigma_implied, churn-free: the *middle* is about right everywhere (robust width median|z|/0.6745 = 0.8-1.15 for every cat), but the **share of forecasts missing by more than two of the sim's OWN sigmas is SVHD 18.2%, QS 10.9%, ERA 9.5%, R 8.0% against the 4.6% a correct model shows**. So defect B is real and still stacks with defect A, but it is excess kurtosis rather than a uniformly too-narrow week -- which matters, because widening sigma globally would be the wrong fix. The tail ranking follows the product: SVHD is worst on all three measures (slope 0.58, |z|>2 at 18.2%, relative gap 42%) and worst in the tail at 8.3x. **(5) Smaller third mechanism, also roster-independent: role classification denies mass to rostered arms.** m70 2026-06-21: Brandon Young was IN the budget as `role=RP, exp_qs=0` and started (GS=1). An RP-classified pitcher making a spot start is a known, accepted case for QS *credit* (the Roki Sasaki decision) but carries **zero projected QS probability**, so a rostered pitcher's real outcome sits outside the sim's support. Same family as `_open_sp_game_weight` (app/sim.py:865) dropping any probable-carrying game from the cadence pool. **(6) The actionable gap: K has no rate shrinkage and now has the tightest-CI margin defect of the three.** Today's empirical-Bayes blends covered QS (`QS_RATE_PRIOR_STARTS` 7.0) and SVHD (`SVHD_RATE_PRIOR_APPEARANCES` 8.0), so those two slopes are **pre-fix and should improve on their own** -- these numbers score the model as it ran and cannot be re-simmed. K's will not improve: nothing was applied to it, and the earlier decomposition showed K's per-start *rate* is fine (-4% against credited starts), so K's 0.63 slope is **start-count driven**, i.e. the same residual the start-count investigation closed as "deliberate activation assumption + All-Star artifact". That closure was about the *level*; this shows the same residual is doing real damage to the *margin*, which the level analysis could not see. **Recommendation, unimplemented:** shrink the projected margin (not the level) on K/QS/SVHD by the measured slope before `_decide` consumes it -- it is one multiplier per cat, measurable, and leaves the displayed projections untouched. Re-measure with the same regression after a fix; unlike the level biases this one IS re-measurable week to week. **Now re-runnable**: `.venv/bin/python scripts/tail_calibration.py` (~35s), measurement shared in `app/tail_calibration.py`, 26 tests. Rebuilding it against the real DB reproduced every headline number and forced the defect-B correction above -- which is the argument for productionising an analysis rather than trusting a scratchpad pass | no -- but the churn exclusion was the owner's steer, and it changed the conclusion: it retired K and promoted the margin-slope defect from "one of three mechanisms" to the main one |
| 2026-08-10 | owner: "past runs had bugs fixed during the season — that could especially explain being near-certain but wrong" | tested the hypothesis with an era split instead of just agreeing | **CORRECT for SVHD/QS, NOT for the general overconfidence — and it materially changes how the WP-calibration result should be read.** Added `--split-at` to `scripts/wp_calibration.py` (default period 14; the confidently-wrong-producing fixes cluster before it: Binomial-not-Poisson 06-07 — literally the "2 QS from one start spuriously wins a locked category" bug — QS `max`-not-add 06-08, SVHD entry/exit margins + spot-starter SVHD skip 06-10, relief-SVHD smear 07-03). Result: **SVHD skill −0.527 → −0.125 and slope +0.03 → +0.41; QS −0.162 → −0.015 and slope +0.00 → +0.37; HR −0.109 → −0.052.** A slope near **zero** means the forecast carried *no information at all*, which is the signature of a correctness bug rather than a modelling weakness — so the headline "SVHD skill −0.301, worse than a coin flip" substantially measures **dead bugs**, and I've qualified that claim in CLAUDE.md rather than leaving it to be read as a property of today's model. **But the general overconfidence survives the control:** pooled slope +0.63 [0.41,0.84] pre vs +0.65 [0.37,0.91] post — unchanged, both excluding 1.0. So "per-category probabilities are too extreme" stands, and the under-dispersed-Binomial hypothesis stays live for the residual. Also noted: **even the "post" era predates today's fixes** — the RP-appearance denominator was live for all of weeks 10-18, grew hyperbolically, and inflated relievers *in proportion to how many a team rostered*, i.e. **asymmetrically across rosters**, which is a textbook mechanism for confidently favouring the wrong side in SVHD specifically. n≈24 per cell so the split is directional, not conclusive. Meta-lesson for the log: this is the third time today a headline number needed an interpretive control before it meant what it appeared to mean (the others: Morejón's accurate-by-cancellation K, and my own QS K=9 from a stale sample + wrong estimator). Measuring is the easy half; the sample and the era are where the errors live | no — owner's hypothesis was right and I confirmed it rather than being corrected |
| 2026-08-10 | wk19 m109 (WAR/Dawgs) + m112 (Mamas/Norsemen) 23:55→00:05 Oslo | ran wp_diff.py on both before any hypothesis; no pre-tool claim | **NOTHING HAPPENED — Monte Carlo noise, and the window is pre-first-pitch.** m109 53.9%→54.2% (+0.2pp), m112 31.4%→31.6% (+0.2pp). Both decompositions: **banked category_state flat, zero budget changes, no games Final, no overlapping flags, no leader flips**, largest per-cat move 1.3pp. Noise scale verified in code rather than assumed: `DEFAULT_SIMS = 10_000` ([sim.py:28](app/sim.py:28)) and the module uses the **unseeded** global RNG (`import random`, [sim.py:19](app/sim.py:19), no `random.seed` anywhere) — so each tick re-draws 10k sims from identical inputs. SE = √(p(1−p)/10000) ⇒ 0.50pp at p≈.54 and 0.46pp at p≈.31, ×√2 for a tick-to-tick difference ⇒ ~0.7pp; observed moves are 0.3σ. The published chart shows m112 dipping to 30.8% at 21:55 then 31.6% at 22:00 (+0.8pp ≈ 1.2σ) — still noise, and worth knowing the *site* wobbles slightly more than the boundary-to-boundary diff suggests. **Context that makes it unsurprising: week 19 had not begun** — all 20 of the 08-10 games read `Pre-Game`(18)/`Scheduled`(2) with **no `game_day_activity.active_start` for any of 08-10/11/12**, so every category avg is projection-vs-projection with nothing banked. Query time was 22:14 UTC, i.e. the owner asked about the *preceding 20 minutes* (00:14 Oslo). Pipeline healthy (newest snapshot 4 min old). The 6 open flags are all week-18 leftovers and unrelated to the window: `INV_EMPTY_BUDGETS` m104/m106 (the documented, still-unresolved end-of-week drain), `ANOM_WP_SWING` m106 −23pp (08-09 settle), `ANOM_SCRAPE_EMPTY` ×1 (08-09 20:20 — the single-tick transient shape, not the auth case) | no |
| 2026-08-11 | morning follow-up: wk19 m109 WAR/Dawgs after the overnight slate | ran wp_diff.py over 00:15→09:40 Oslo before attributing; resolved the one line that didn't add up instead of hand-waving it | **WAR 63.1% → 77.9% (+14.8pp) — live play, and the Dawgs' half of the night was empty.** Banked deltas (verified): **WAR 6 R, 7 H, 1 HR, 2 SB, 12 K, 37 outs, 1 QS, 1 SVHD; Desert Dawgs 5 H from 21 AB, 0 R, and ZERO innings pitched** (no OUTS/K/QS/SVHD banked at all). Two leader flips dominate: **R +26.5pp** (WAR's projected total *rose* 35.55→36.70 on 6 banked runs while the Dawgs' fell 36.32→31.98 — banked 0 plus a game off the remaining slate) and **SVHD +24.5pp** (WAR 3.60→4.12). The SVHD line initially looked wrong — WAR's total rose while all four of its relievers' budgets fell (Varland 1.93→1.68, Lee 1.0→0.83, Ashby, Taylor; remaining sum 3.60→**3.13**) — resolved by querying banked state: **WAR banked SVHD=1**, and 3.13+1.0=4.13 ≈ the 4.12. So it's real production, not an artifact. Also **QS +20.3pp**: WAR banked 1 while the Dawgs' projection collapsed 2.89→1.91 as Sheehan (`benched-live-drop`) and Eovaldi (`cadence`) starts **evaporated without being pitched** — with 0 banked innings those were losses, not bankings, and Skenes shed a cadence turn (1.34→1.0 units) on top. K +8.3pp → **99.9%** (locked; Dawgs' projected K 46.65→31.73). Counter-moves against WAR: **ERA −17.4pp** (their pitchers got hit — banked ERA 7.297 over 37 outs, projected 3.81→4.54, while the Dawgs stayed clean at 3.47 by not pitching), WHIP −5.2pp, OPS −5.5pp (leader flipped to the Dawgs). **Is Williams still dominant? No — overtaken by live results, but still load-bearing:** the Dawgs project 3.80 SVHD without him and SVHD has flipped to WAR; *hypothesis:* with his ~1.57 they'd project ~5.4 and would still lead the category, so his IL stint is a necessary condition for the flip while the proximate cause was WAR banking one. The league-wide unit reductions (hitters 7.0→6.0, SPs 1.88→1.0) are just Monday coming off the slate — expected, not an anomaly. One **35-min tick gap 04:45→07:30 UTC** (documented laptop-sleep/dark-wake pattern) but WP barely moved across it (78.1→77.9), so no meaningful backlog. **`batter_final_lines` verified across the full slate: 205 lines / 10 games / 18-25 batters per game / zero NULL components / league AVG .245 (666 AB, 163 H, 18 HR, 95 R, 18 SB)**, against 80 pitcher lines over the same 10 games, all 20 team-rows now `Final`. No new flags overnight — the 6 open are the same week-18 leftovers (INV_EMPTY_BUDGETS m104/m106, ANOM_WP_SWING m106, ANOM_SCRAPE_EMPTY 08-09). **Process note:** I also mis-stated the wall-clock time earlier in this session (carried a `date` reading forward ~5.5h and scheduled a cron for a moment already past); owner corrected it. Read the clock, don't extrapolate it | no — but owner corrected my stated local time |
| 2026-08-11 | late-credit probe verdict: does ESPN post a QS/SVHD credit promptly when the game earning it is the LAST one running? | probe run first; then corroborated the mechanism in fast.log before accepting the verdict | **PASS — Phase 2 (deleting the QS/SVHD floor) is UNBLOCKED.** The 2026-08-10 slate finally exercised the closing batch. Four late credits, all from games finalizing with **nothing else in progress**, every one banked within seconds: Tanner Scott SVHD + Edwin Díaz SVHD (final 08-11 04:50:02 → banked 04:50:11, +0.1 min); **Reid Detmers QS + Jacob Latz SVHD (final 08-11 05:05:02 → banked 05:05:10, +0.1 min)** — that last pair is the exact case the floor exists for. Mechanism corroborated in `fast.log`: the 05:05:00 tick ran with `in_progress=0` and logged `live-scraped 120 category cells` — the **closing scrape**, not a live-game scrape — and the window then behaved (scraped 05:05/05:10/05:15/05:20, SKIPPED from 05:25, i.e. exactly 20 min). So ESPN posts the credit within ~8s of Final even with nothing live, and the closing scrape is there to read it. **Two self-inflicted detours worth recording.** (1) I first read the *2026-08-10* log for an event whose `final_at` is *2026-08-11*T05:05:02 — a game dated D finalizes in the early hours of D+1 UTC — and briefly concluded the scrape had been SKIPPED and that Phase 1 had a period-rollover bug. It does not; I was reading the wrong day. **Fixed the probe to print MM-DD with every timestamp** (it printed bare times, which is what invited the error) plus an explicit warning line. (2) Earlier, three bugs in the probe itself were only caught by running it on real data — the `team_schedule` period lookup (pruned for past weeks), a `landed_at` baseline taken *after* the Final (hid the fast case as "NEVER BANKED"), and a non-indexed join that whole-scanned 2.6M rows per credit (>2 min → 0.2s). Health otherwise quiet: 139/145 ticks completed since 20:00Z (6 incomplete, no lock-skips), site fresh (local and live both 08:00:42Z, 1.0 min), 0 deploy runs stuck in `waiting`, flag board unchanged (INV_EMPTY_BUDGETS m104/m106 still deliberately open, one ANOM_WP_SWING, one single-tick ANOM_SCRAPE_EMPTY) | no |
| 2026-08-11 | Phase 2: delete the QS/SVHD floor reconstruction | user greenlit after the probe PASSed and after I measured (and corrected) the impact claim | **Done — 555 lines removed, 102 added, across sim.py / cli.py / validate.py + 4 test files.** Removed: `sim.load_settled_floor`, `_count_qs`, `_count_svhd`, the whole QS/SVHD block in `reconcile_live_components` (and its `settled_floor` param, and the now-unused `matchup_id` on `apply_live_components`), `cli._apply_qs_svhd_floor` + call site, `validate._supported_credit` + `INV_SITE_QS_OVERCREDIT` + `QS_OVERCREDIT_TOL`. QS/SVHD now pass through from `category_state` — one source of truth. **Validation got STRICTER, not weaker:** dropping 63/83 from `_LIVE_RECON_CATS` means `INV_SITE_DB_MISMATCH` now checks them directly against ESPN's number with nothing derived in between. **Impact measured before doing it, and it corrected my own earlier claim:** I had justified this partly on "the floor over-counts ~8%", from week 17's 2-of-24. Week 18 came in **24/24 correct**, so it's 2 of 48 (~4%) and zero in the most recent week — accuracy was NOT a good reason. The real case is volatility (the m105 +16.4pp/−8.6pp pair came from a box credit unlocking an already-earned floor, landing a +1 credit as +2) and complexity (four consumers, four gates, source of every QS/SVHD incident this week). Numerical impact today: **0 of 24 (team,cat) pairs change** — the closing scrape keeps ESPN current, so floor+box no longer exceeds the scrape anywhere. Verified: 468 tests pass, golden week clean, `check_published_site` against the live artifact returns **no findings**, week-18 published vs ESPN matches **24/24**, and a real `compute`→`publish`→`validate` cycle ran with 0 errors. Two tests were rewritten rather than deleted so their lessons survive: `test_publish_does_not_adjust_qs_svhd` now asserts the *absence* of any display-side raise (and that both deleted symbols stay gone), and the daily-lineups slot test was repointed from the floor to the surviving consumer (the ERA/WHIP/OPS component reconstruction, where a wrong slot now silently mis-attributes innings instead of publishing a phantom credit). Residual risk accepted: if the scrape dies, QS/SVHD have no fallback until the 07:00 settle — `INV_SCRAPE_STALE` is the detector, and during the actual Akamai outage the floor didn't help anyway | no |
| 2026-08-11 | look into INV_EMPTY_BUDGETS (3rd recurrence after f31ace2d) | evidence-first; the assumed cause was wrong and the timing is what exposed it | **ROOT-CAUSED AND FIXED — and it was never the end-of-week drain everyone assumed, me included.** The flags fired **23:36→02:36, MID-SLATE**, and stopped at the last tick before game 823268 went Final at 02:40:14 — the slate itself ran to 02:40. Checking the timing first is what broke the assumption; every prior triage (2026-07-29, and my own note on 08-10) had filed it as the Sun→Mon drain. Only 2 of 12 sides flagged: m104 **home** (WAR) and m106 **away** (That Bus). Their opponents also hit 0 budgets and did NOT flag, so the benign gate worked for them — the discriminator is `_side_remaining`. Each of the two had exactly **ONE** active-slot player left in a live game — **Peter Lambert (slot 14)** and **Jason Adam (slot 15)**, both relievers, both in game 823268 — while all 17 of their other active players' games were already Final. `_side_remaining` counted that non-Final game as a full remaining game; the sim gives an underway game a **factor**, and `_rp_factor` ramps to ~0 once a game is deep in the bullpen window, so `_make_budget` dropped both and the side legitimately projected nothing ⇒ `remaining>0` with 0 budgets ⇒ gate never engaged. **Fix:** `_side_remaining` now also skips games already underway (`current_inning is not None`), counting only games that have not started — where "the sim will project something" is unambiguous. Third filter; `f31ace2d` added the past-date guard and `_is_playable` and was genuinely incomplete. Still caught: `roster_n == 0`, and empty budgets with a not-yet-started game. **Accepted gap:** a real projection failure occurring when *only* in-progress games remain. Regression test `test_side_remaining_ignores_a_game_already_underway`, verified to fail with the fix reverted. **Method note worth keeping:** `team_schedule.became_final_at` is UNUSABLE for a past period — `refresh-schedule`'s DELETE+INSERT re-stamps every row to one time (all 18 of WAR's players showed an identical 04:05:14, which briefly led me to conclude all their games were live). Use `pitcher_final_lines.final_at`, the write-once archive, for any historical "when did this game finish" question | no |
| 2026-08-11 | morning-check flag triage: ANOM_WP_SWING m106 + ANOM_SCRAPE_EMPTY (both 2026-08-09) | ran wp_diff before attributing, per contract | Both benign, both resolved with notes. **ANOM_WP_SWING m106** (20:35:49→20:45:44Z, home 51.5%→29.2%): dominant term is **HR, win share 78.7%→36.5% (−42.2pp)** on That Bus banking one real home run (banked HR 3→4, lifting their projected HR 3.48→4.41); second is a **WHIP leader flip** (−10.8pp, Bear Nation 1.089→1.11 on live baserunners). Confirmed in banked `category_state` alongside H 52→53 and R 21→22 — a 20-game slate banking counters live. Not a data fault: every budget delta is fractional in-progress decay, no game went Final in-window, no compute gap, all four `live_recon` groups `matched` on both boundaries. Textbook overshoot-and-correct (already back to 37.1% by 21:06; the away side won the matchup). **ANOM_SCRAPE_EMPTY**: the 20:20:55Z fetch wrote 144 cells (REST components only) against a steady 263 on every neighbouring tick 20:00–21:05, recovering at 20:25:17 — one transient miss, not an auth wall, so no `espn_auth_setup.py` re-run. Cell-count-per-tick is a good cheap discriminator here: a real auth failure holds at 144 | no |
| 2026-08-11 | why fast.sh ticks were taking 17 min (525s fetch, 1046s publish) | I misread the symptom and made it worse before finding the cause | **Root cause: the `ustats event-tune` job in ~/git/ultimate-stats was running at `nice -n 5` with no `taskpolicy -b`** — a recurrence of the 2026-08-09 starvation, and proof that **nice 5 is not enough** (the standing rule is 19 + background QoS). `renice 19` + `taskpolicy -b` on its 7 PIDs fixed it immediately: the next `validate` step went **51s → 1s**. **My error, worth remembering:** at 10:20Z I saw a tick 10 min old and deleted `.app.lock` as stale. It wasn't — that tick was alive and legitimately holding it, merely starved (its `fetch` took 525s). Deleting the lock let the next tick start on top, so two `publish` processes then contended on a 1.6 GB DB. **The lesson is the diagnostic order:** before judging a lock stale, check whether the holder is *alive* (`kill -0`) and whether the box is *starved* (`uptime`, and `ps -eo ni` on the heavy job) — a slow tick and a wedged tick look identical from the lock file alone, and `_common.sh` already encodes the right test (`MAX_LOCK_AGE` steals only from a dead or genuinely-wedged holder). Both ticks drained on their own once the CPU freed up | no |
| 2026-08-12 | morning check: late-credit probe for 2026-08-11 (game date) | the probe is no longer a gate — Phase 2 shipped 2026-08-11, so this now REGRESSION-tests code already in production | **PASS, and the strongest evidence so far — this is the first night the probe caught the decisive case.** 14 rostered credits, **2 of them LATE**: Blake Snell (QS, Swamp Dragons) and Michael Wacha (QS, Jo Mamas), both from the night's final game finalizing at 05:05:02Z with **nothing else live** — both banked at 05:05:13Z, **+11 s**. The 12 mid-slate controls all banked in 9-14 s. Prior confirmation (2026-08-10) had four credits at ~8 s but *no* closing-batch case; 2026-08-09 was 11 mid-slate credits and `NOT EXERCISED`. So the premise the deleted QS/SVHD floor rested on — ESPN credits promptly once nothing is in progress — is now confirmed on the exact scenario the floor existed to bridge, not merely inferred from mid-slate behaviour. Keep `scripts/late_credit_probe.py` as the standing check before any QS/SVHD adjustment is ever reintroduced | no |
| 2026-08-12 | what caused the Melonheads WP jumps Wed 00:05 and 06:25 Oslo? (m113, Melonheads are AWAY) | ran wp_diff on both windows first; both turned out to be OPPONENT budget removals, neither was live play | **Both jumps are That Bus losing a player from its projection — the Melonheads themselves did nothing.** **(1) 00:05 Oslo = 22:05Z, Melonheads 72.3%→82.5% (+10.2pp): Kyle Stowers hit the IL.** `medium.sh` ran `refresh-rosters` at 22:02:00Z (injuries 266→**268**) and the next fast tick recomputed. `player_injuries.return_date` = **2026-08-21**, past period 19's end (08-16), status TEN_DAY_DL ⇒ every remaining game precedes his return date ⇒ budget dropped whole (units 6.0, exp_h 5.4, exp_r 2.9, exp_hr 1.08). Category evidence matches almost exactly: home projected H 58.23→53.04 (−5.19 vs his 5.4), R 32.89→30.08 (−2.81 vs 2.9), HR 9.24→8.12 (−1.12 vs 1.08); win shares R −13.5, H −12.3, HR −9.6pp. Banked `category_state` **flat** ⇒ not live play (playbook #14). Note the tool marked SB `LEADER FLIPPED` but it moved only −2.0pp — the flip heuristic is not the driver here, the three counting cats are. Also **near-zero optimizer backfill** (the drop is ~96% of his raw line), which is the exception to playbook #14's "impact is marginal"; *hypothesis:* no spare eligible bat on the bench. **(2) 06:25 Oslo = 04:25Z, Melonheads 87.5%→93.3% (+5.8pp): Hunter Brown's phantom cadence start collapsed when his real start went Final.** Verified: Brown is **bench-slotted (16)**; game 823186 `became_final_at` = **04:25:02Z** and the budget vanished at the 04:25:32 tick; home projected K fell 47.74→41.74 = **exactly** his exp_k 6.0, QS 2.82→2.34 ≈ his exp_qs 0.46. Mechanism read in code: `_drop_inprogress_for_benched` (`sim.py:1151-1152`) filters **only** `"In Progress"` games, so while his start was live it was hidden from his own schedule view — and `_cadence_extra_start_dist`'s anchor loop (`sim.py:831-838`) therefore never saw it, leaving the anchor at his last *recorded* start **2026-08-05** (`pitcher_starts` confirms) and letting the rest-day walk project a phantom extra turn (units 0.98, flag `cadence`). At Final the filter stopped applying, the anchor jumped to **08-11**, and with every remaining Astros game already carrying a probable (08-12 King, 08-14 Blanco, 08-15 Lambert, 08-16 Javier — the last two `fetched_at` 04:02:01Z, i.e. before the drop) `open_dates` was empty (`sim.py:846`) so the walk put all mass on 0 extra starts (`sim.py:867-868`) ⇒ budget removed. Secondary term: Melonheads banked H 21→23, R 9→10 live in the same window. **LATENT ARTIFACT WORTH FIXING (new, not previously recorded):** the benched filter is meant to zero a locked-out game's *scoring* contribution, but it also erases that game's *rotation-phase* information, so for the entire duration of a benched SP's start the model credits him a phantom extra turn — the very turn he is making — and only self-corrects at Final. Direction is a consistent over-projection of the benching team's K/QS while the game is live | no |
| 2026-08-13 | Swamp Dragons WP, Thu 02:25-03:25 Oslo (m110, Dragons are AWAY: 61.0% -> 78.0%, +17.0pp) | ran wp_diff first; no single event — cumulative live play, with SVHD dominant | **All live play, verified**: banked `category_state` moved on both sides, no game went Final in-window, no compute gap, every `live_recon` group `matched`, and every budget delta is fractional in-progress decay rather than a lineup move. Ranked by |Δ win share| (home = Sox Teacher, so negative = Dragons gaining): **SVHD −40.1pp** is the single biggest — the Dragons' projected SVHD went 4.23 → 6.03 (+1.80), *entirely* from two relievers entering save/hold situations mid-window and picking up the `svhd-ingame` override (Dylan Dodd exp_svhd 0.20→1.15, Erik Sabrowski 0.78→1.63; +1.80 combined, matching the projection move exactly), while Sox Teacher's Fairbanks went the other way 0.71→0.58. Then **HR −22.9pp (LEADER FLIPPED)** on banked HR 3→5, **OPS −13.1pp (LEADER FLIPPED)** on .797→.892, **R −6.3pp** (16→19 vs 11→12) and **H −5.8pp** (16→19 vs 23→25). One genuine counter-current: **QS +10.1pp back toward Sox Teacher** as Foster Griffin's start ended (units 0.25→0, exp_qs 0.84→0, flag → `live-keepalive`), dropping Dragons' projected QS 4.77→3.93. The biggest single tick (02:45 Oslo, −9.0pp) is itself multi-category — SVHD −12.7, HR −11.3 w/ leader flip, OPS −7.1, R −5.3 — i.e. an HR landing while a save situation firmed, not one event. **Did NOT mean-revert** (the usual overshoot-and-correct caveat): 78.0% at 03:20 held and extended to **86.9%** by 09:20 Oslo | no |
| 2026-08-13 | "small step increase to WAR's WP Wed 20:25→20:35 Oslo" (m109, WAR is HOME) | asked instead of forcing a fit — the premise did not match the data | **No increase exists in that window**: 87.0% (20:25) → 87.0% (20:30) → 86.9% (20:35) on the published series, i.e. flat within MC noise (SE ≈ 0.34pp at p≈0.87). Did not attribute anything to it. The nearest real move is **+8.1pp over 20:40→20:50** (86.9 → 88.7 → 92.6 → 95.0) — a large climb, not a small step; other candidate "small steps" that evening are +1.2pp at 20:00 and +1.5pp at 22:25. Went back to the owner with the three candidates rather than guessing which chart/window was meant (per feedback_ask_dont_guess_references); he re-asked as **20:20→21:05**, which was the right call — see the next row. **Standing reminder this reinforces:** when a question's premise doesn't survive step 0, say so — do not reach for the nearest movement and narrate it as the answer | no (asking was correct) |
| 2026-08-13 | WAR WP, Wed 20:20-21:05 Oslo (m109, WAR is HOME: 87.4% -> 94.0%, peak 95.0% at 20:50) | ran wp_diff; two rate-cat leader flips carry essentially the whole move | **The Desert Dawgs' starter got hit hard — that's it.** ERA **+28.0pp (LEADER FLIPPED)** and WHIP **+23.9pp (LEADER FLIPPED)** are ~52pp of win share between them: the Dawgs' banked ERA went **3.00 → 6.75** and WHIP **1.50 → 2.00** (projections 3.53→4.51 and 1.29→1.45), while WAR's own pitching improved in the same window (banked ERA 5.87→4.91, WHIP 1.435→1.309, K 15→19, R 12→13, SB 4→5) — the two crossed. Third term QS +6.7pp, largely Shane Baz's exp_qs dying **0.49 → 0.0** (units 0.85→0.48). No roster move, no game Final in-window, no compute gap, all `live_recon` groups `matched`. **Incidental finding, flagged not chased:** WAR's Peter Lambert **doubled** in that tick — units 1.0→2.0, exp_k 5.1→10.2, exp_qs 0.44→0.88, exactly 2×. Not physically real: last recorded start 08-08, and at `MIN_REST_DAYS` 5 the period (ends 08-16) allows **one** more turn; he reads 1.0 again now. *Hypothesis (unverifiable — `team_schedule` is current-state):* a transient double-assignment of Lambert as probable on two dates, which `_probable_starts_for` simply sums — plausible given the feeds moved him (08-15 yesterday, 08-14 today). Note `_max_remaining_starts` would NOT catch this: it clips only the extra/cadence piece and always respects announced starts. Did not drive the WP move (K already 100%, only QS touched) and is unrelated to the 2026-08-12 cadence-anchor fix, which touches the anchor rather than announced starts. **If a starter reads ~2.0 units mid-week again, snapshot `team_schedule` while it is live** — that is the only way to confirm this one | no |
| 2026-08-13 | could the recent changes have caused the Lambert/Gore start doubling? | **No — ruled out three independent ways, and the real mechanism is now reproduced.** I had asserted this without testing it; the owner was right to ask | **(1) It predates the fix.** Counting SP budgets at `units >= 1.8` *where the days left in the period cannot physically fit a second start* (`< MIN_REST_DAYS`): **447 instances over 9 days BEFORE** the 2026-08-12T10:25Z cadence-anchor fix, including ~25 distinct pitchers on 08-09 alone (period 18's last day, where 0-1 starts were possible). Post-fix is 112 over 2 days — a comparable per-day rate, so the fix neither caused nor cured it. *Method note:* my first cut counted `units >= 1.8` with no window filter and got 6003 — but Mon/Tue of a period legitimately supports a two-start week, so that number is mostly real. Same over-broad-metric error I made counting `benched-live-drop` (52,637). **Filter to what is physically impossible before calling anything an anomaly.** **(2) The reproduction is anchor-independent.** Two announced probables one day apart → `units=2.0, exp_k=12.0`, *identical* with and without a recorded start, i.e. regardless of what `_rotation_anchor` has to work with. That matches Gore's observed 00:00Z budget exactly (units 2.0, exp_k 12.0). **(3) Disjoint code path.** The doubling is `_probable_starts_for` summing `_sp_factor` over both announced games; the fix only changed how the *cadence* anchor is resolved, which feeds `_cadence_extra_start_dist` — a different term of `units`. **Real mechanism:** two announced probables for the same pitcher closer than `MIN_REST_DAYS`. `_max_remaining_starts(08-10, 08-16)` = **1**, but `_cap_extra_dist` folds only the cadence distribution — announced starts are *always* respected by design (deliberate, from the 2026-06-26 Rasmussen fix), so the cap **structurally cannot** reach this. **Source, hypothesis:** the ESPN probables overlay filling a game MLB had not yet named, with MLB later naming someone else — Gore appeared at 2.0 on a 00:00Z `refresh-live` tick (which fills probables) and dropped back to 1.0 at 04:05Z, the first tick after `refresh-schedule` at 04:02Z. Consistent with CLAUDE.md's note that ESPN's early probables are tentative. Unprovable after the fact: `team_schedule` is current-state. **Suggested guard (not implemented):** reject an announced start within `MIN_REST_DAYS` of the same pitcher's other announced start, preferring the MLB-sourced row (`probable_pitcher_mlbam_id` non-NULL) — this kills impossible pairs while leaving a legal two-start week (e.g. Jun 23 + Jun 28) untouched; plus a validation check for a projected start count above the physical bound, which the battery currently has no equivalent of | no |
| 2026-08-18 | Bear Nation WP vs WAR, today 06:00-06:05 Oslo (m116 period 20, Bear is HOME: 6.7% -> 10.8%) | ran wp_diff; banked flat + no leader flips ⇒ projection change, and the trigger is bracketed to the minute | **The daily `refresh-schedule` announcing second starts.** Last tick with old values **04:00:15Z**, `daily.sh` ran **04:02:00-04:02:32Z**, new values at **04:05:26Z** — the first daily run since period 20 became the live week, so its DELETE+INSERT pulled fresh probables for the new current period. All four relevant SPs sat at an identical **1.34 units** before (≈1 announced + ~0.34 cadence extra); after, Bear's **Bryce Miller** and **Kyle Leahy** went to exactly **2.0** (second start announced) while WAR's **Zack Wheeler** and **Braxton Ashcraft** went to exactly **1.0** (open tail filled by other pitchers). The round 2.0/1.0 values are the tell — *announced* starts, not cadence estimates; my first hypothesis (flat-fallback → cadence) was wrong and the provenance flags disproved it: all four read `cadence` on BOTH sides of the tick. Effect: Bear projected K 62.60→69.45 vs WAR 58.42→54.17 ⇒ **K +29.4pp** win share (61.9→91.2) and **QS +15.3pp**; everything else <3pp. **Explicitly checked against the 2026-08-13 Lambert/Gore impossible-pair defect and it is NOT that**: Miller and Leahy each have probables on **08-18 and 08-23, exactly 5 days apart = `MIN_REST_DAYS`**, so both two-start weeks are physically legal (the defect signature is a gap *below* 5). **Caveat worth carrying:** those 08-23 probables have `probable_pitcher_mlbam_id` NULL ⇒ ESPN-overlay, i.e. ESPN guessing the rotation turn 5 days out, beyond MLB's ~3-day horizon and documented as tentative. Bear's whole +4.1pp rests on two unconfirmed second starts and reverses if either slides or is skipped; their 08-18 starts are MLB-sourced. **Reusable signature:** a same-direction jump in *several* SPs' units at ~04:02-04:05Z, with banked stats flat, is the daily schedule refresh — especially on the first daily run after a period rollover, when a whole week of probables lands at once | no |
| 2026-08-18 | Norsemen WP ~04:15-04:45 Oslo (m117 period 20, Norsemen HOME: 60.2% -> 75.0%) | ran wp_diff, then isolated the dominant tick rather than attributing across the whole window | **Live play, concentrated in ONE tick: 02:25:27→02:30:27Z, +13.2pp.** What makes it violent is **four simultaneous leader flips** — SVHD **+17.5pp**, ERA **+9.7**, WHIP **+9.3**, H **+8.6** — plus HR +10.6. Two things at once: the Norsemen banked **H 5→7, HR 2→3, R 5→7**, while Sox Teacher's pitching was hit hard enough to take their banked **ERA 1.227→3.522** and **WHIP 1.636→1.957**. Largest single term is SVHD and it traces to one player: Sox Teacher's **Ryan Zeferjahn, exp_svhd 1.67→0.82** (−0.85, matching the away side's −0.83 projected SVHD almost exactly) — the in-game model reading his save situation collapsing, and it was right: his archived Final line is **3 outs, 2 ER, sv=0, hld=0**. Genuine throughout: no games Final in-window, no roster moves (all budget deltas are fractional in-progress decay), every `live_recon` verdict `matched`, no flags. **Caveat on how much to read into it:** day 2 of the period, so the denominators are tiny — an ERA moving 1.23→3.52 is a few earned runs over ~10 IP. Early-week rate cats are extremely volatile and that is *why* four flipped together rather than one; expect churn, not a settled state. **Did NOT mean-revert** (the standing caveat for a mid-slate swing): 75.7% at 02:30Z → 79.0% by 03:00Z, and sitting at 76.5% nine hours later. **Method note:** the whole-window decomposition spread the cause across six categories and looked diffuse; isolating the single biggest tick is what surfaced the four flips and the one player behind the largest of them | no |
| 2026-08-18 | Teacher SVHD WP 51.4% (03:55) → 35.8% (04:10) → 51.9% (04:30) Oslo — what caused the round trip? | premise was mirrored; corrected it and answered the underlying event, which was unambiguous | **Those three numbers are the NORSEMEN (home) column, not Sox Teacher's** — Teacher's own SVHD went 28.9→43.6→27.3. Same event either way. **Cause is one player: Sox Teacher's RP Ryan Zeferjahn**, exp_svhd **0.96 (no flag) → 1.67 (`svhd-ingame`) → 0.82 (`svhd-ingame`)**. Teacher's team total tracks him almost exactly (5.92→6.57→5.59) while the Norsemen's barely moves (6.49→6.40→6.19) — entirely the opponent's bullpen. Arc is `_override_rp_svhd` behaving as designed: flat season-rate share before he pitched → high conversion probability the moment he entered a save situation → dropped when he left without it. Model read him right: archived final line **3 outs, 2 ER, sv=0, hld=0**. End state 0.82 correctly sits *below* the 0.96 start — an appearance spent for nothing. Large category swing from ~0.8 projected SVHD because that cat sits near 50/50 with ~20% ties, the most projection-sensitive on the board. CLAUDE.md lists this shape under owner-known benign behaviours (playbook #8). **False alarm worth recording:** mid-investigation I found `reliever_appearances` **empty league-wide** and briefly concluded the 2026-06-10 entry/exit-margin fix had broken (which would mean every save/hold judged on the flickery live-margin fallback). It has not — there is a documented prune at `cli.py:1133` dropping rows once a game leaves the unsettled window, and with `SETTLE_LAG_HOURS=7` the boundary was 2026-08-18, so 08-17 games aged out on schedule. **Check the prune before calling an empty table a regression.** **Standing limitation this exposes:** because those rows are ephemeral, it is impossible after the fact to tell whether a given SVHD judgement used a locked entry/exit margin or the live-margin fallback — the observed sequence is consistent with both. Archiving entry/exit margins the way `pitcher_final_lines` archives box lines would close that, if it ever matters | no (premise mirrored, corrected) |
| 2026-08-24 | owner: "bug in OPS for Giraffes-Bus — sim had Bus winning OPS at 100% for hours after all games were over, until the settle" | owner was exactly right; a second session (no DB access) independently predicted the verdict/rates from code alone, and the DB confirmed it to the digit | **CONFIRMED BUG, two defects chained.** m120 sat at home_wp **0.0%** from 02:20:32Z (first tick after the last game went Final at 02:15:03Z) to **07:00:15Z**, when the REST settle flipped it to **100.0%** — **4h40m of a 100%-confident WRONG published winner**. OPS was the sole decider: 06:55Z GIR 4-BUS 5, 07:00Z GIR 5-BUS 4. **Defect A (cause) — `_norm_name` COLLISION.** Two active MLB players named Max Muncy normalize to one key; the box-score pool holds every player, so both lines matched the single rostered Muncy and both were summed: `mlbam 571970 team 19 AB=3` (rostered) **plus** `mlbam 691777 team 11 AB=4` (not rostered). Because the OPS group adds AB/2B/3B/BB/HBP/SF but **not** H/HR (the scrape owns those), the injection was **+4 AB and +0 H** — pure denominator inflation: H=56/AB=237 → 0.6387 instead of H=56/AB=233 → 0.6491. CLAUDE.md had flagged this risk in one line ("two names colliding after normalization... rare"); first time it bit. **Defect B (amplifier) — the guard laundered it.** `_judge_group` accepted the reconstruction because |0.6387−0.6491| = 0.0104 < `LIVE_RATE_TOL[OPS]` = 0.012 → verdict `matched` → committed, **discarding the exact scraped 0.6491**. The head-to-head margin was **0.0043**, so the tolerance was **2.8× the margin that decided the category** and the committed value landed on the wrong side of the Bus's 0.6448. **Tightening the tolerance would NOT have helped** — both `matched` and `closer` commit, and the only non-committing verdict (`baseline`) would have used the raw REST value of **0.7482** (scrape-current H over stale AB=196), far worse. **Fixed both:** (A) `_sum_counted` takes `team_by_norm_name` and drops a line only when both `pro_team_id`s are known and disagree (fails open); `load_unsettled_lines` now selects `pro_team_id`, which the tables already had. Verified on the real archived lines: AB 41 → **37**, exactly ESPN's settled delta. (B) new `INV_SETTLED_RATE_DRIFT` — with nothing left to play, projected rate must equal banked; replaying the incident state it fires, i.e. it would have caught this at **02:20Z**. **Blast radius check:** of the six period-20 matchups only m120 flipped, and its home side was the only one of twelve with a non-trivial OPS reconstruction error — it needs error-under-tolerance AND margin-under-error AND that cat deciding. **Still open (deliberately deferred):** the structural fix — anchor rate cats to the scrape and use the reconstruction only for the remainder — which would make the level immune to any reconstruction error rather than just this one. Note ERA's tolerance is 0.20 and WHIP's 0.04, both coarser relative to typical margins than OPS's 0.012 | no |
| 2026-08-24 | owner: "is it a timezone thing?" — INV_SP_STARTS_IMPOSSIBLE fired on m123 (Misiorowski) and m125 (Sale), both 2.00 starts where 1 fits | new check caught a real defect on its FIRST day in production; owner's date-alignment instinct was right, the timezone mechanism was not | **Not a timezone artifact — the two dates are different games vs different opponents** (Misiorowski 08-27 pk=823581 vs team 21 away, 08-28 pk=823744 vs team 13 home; a TZ shift would repeat one `game_pk`). **Real cause: MLB and ESPN disagree about which DAY the starter goes, and fill-only merging cannot see the conflict.** Queried both feeds live: for the Brewers they agree on 08-25 (Harrison) and 08-26 (May), then MLB says **Misiorowski 08-27** while ESPN slots **Gasser** there and pushes **Misiorowski to 08-28**. Identical shape for Atlanta (MLB Sale 08-27; ESPN Holmes 08-27, Sale 08-28). Since MLB had not named 08-28, `_overlay_espn_probables` filled ESPN's guess → same pitcher probable on consecutive days → `_probable_starts_for` summed both → 2.00 starts. Nothing downstream could catch it: `_cap_extra_dist` clips only the *cadence* piece and **always respects announced starts** by design. Diagnostic tell: the phantom always carries `probable_pitcher_mlbam_id = NULL` (ESPN overlay) beside an MLB-sourced row. **This is the same defect hypothesised on 2026-08-13 (Lambert/Gore) but unprovable then** because `team_schedule` is current-state — the new check bought the live catch. **It also confirms the risk I flagged on 2026-08-18** when widening `refresh-live`'s window took overlay fills ~27 → ~97/tick: more of ESPN's speculative tail ingested, which is where the disagreements live; it surfaced within a week. **Fixed:** the overlay skips a fill when that pitcher is already probable for the same team within `MIN_REST_DAYS`; MLB always wins (only the fill is skipped), accepted fills join the claim set so ESPN cannot duplicate inside its own tail, and the day is left genuinely open for the cadence model. Verified on the live feeds (both phantoms gone, every legitimate fill kept) and league-wide `espn_probables_filled` **97 → 84**, i.e. ~13 conflicting fills suppressed — far more than the 2 that happened to cross the physical bound. Playoff odds re-run after the fix: Norsemen P(champ) 30.6% → 30.0% | no |
| 2026-08-31 | m124 wk21 Sox Teacher–Shih Tzus: app called Shih Tzus the winner overnight, Sox Teacher actually won | ran wp_diff on the correcting tick first; user's SB hunch confirmed by the leader-flip line | **A phantom Shih Tzus stolen base, banked live and entrenched by the monotonicity guard, flipped the whole matchup decision through the hits tiebreaker.** Chain, each step verified: (1) At 04:15:13Z Sat 08-30 the scrape banked Shih Tzus SB 7→8 during ARI's doubleheader game 2 (pk 823176, the ONLY game live at that tick; finalized ~05:00Z). Corbin Carroll — the only Shih Tzus hitter in that game — has **sb=0 in the write-once final box** (`batter_final_lines`), and reconstructing the whole week from archived finals × `daily_lineups` active slots gives **exactly 7** SB: the 8th never existed at Final. *Hypothesis:* ESPN live-credited Carroll a steal that official scoring removed before Final (e.g. rescored defensive indifference); alternative (scrape misread) can't be excluded — `live_batters` is pruned and rejected guard writes aren't logged. (2) The guard (`cli._write_category_score`, cli.py:53-56) rejects any counting read below last-good for the current period — BY DESIGN, and normally correct — so when ESPN's own numbers dropped back to 7 (at latest Sunday's ~07:00Z settle), every lower read was refused. SB=8 stood ~27h. (3) With SB 8-7 away, `sim._decide` (sim.py:1810-1840) scores 5-5 → hits tiebreaker → Shih Tzus 68-48 → AWAY wins; once every category locked (~23:15Z Sun) the sim went **0.0% home for 7h50m** — internally consistent, wrong input. (4) At 07:05Z Mon the current period flipped to 22, period 21 became past → `_write_noncurrent_score` (no guard, "rare late ESPN correction still lands", cli.py:61-74) wrote SB=7 → 7-7 tie → **5-4-1 HOME** → 100%, agreeing with ESPN's own winner. Site is correct now; nothing to repair in the DB. **Collateral:** playoff-odds runs archived 23:15Z–07:05Z priced m124 as a Shih Tzus win (finale-live cadence = every 30 min), so the odds-over-time chart has a real kink there; left as-is (archive is history, not hand-edited). **Why zero error flags:** `ANOM_WP_RAIL_FLIP`'s window is FLAP_WINDOW=6 ticks ≈ 30 min, rails touched ~8h apart; `INV_BANKED_REGRESSED` would only have seen the 8→7 *correction* (and by then the period was past, outside the battery's scope) — the wrong state itself was stable/fresh/self-consistent, the documented structural blind spot (same class as m120 OPS 08-24). **Open gap, not fixed here:** nothing compares a stored current-period counting cat against a LOWER value both sources now agree on — the guard has no "ESPN has settled lower, stop ratcheting" escape. A detector shaped like INV_SETTLED_RATE_DRIFT but for counting cats (fire when scrape+REST both read below stored for N consecutive ticks) would have caught this Sunday morning. Only warn-grade ANOM_WP_SWING fired (×3, incl. the +100pp correction) | no — user's SB suspicion was the cause |
| 2026-08-31 | (correction to the row above) which game/player was m124's phantom SB? | Carroll, ARI@SF game 2 (7-2 SF), "the ONLY game live at that tick" | **Wrong player, wrong game — it was Bryce Harper, PHI@LAA (PHI won 4-2 in extras), top 8th.** THREE games were still live at 04:15Z league-wide, two with a Shih Tzus active hitter (Carroll 823176, Harper 823986); the "only game" claim came from checking final times for just 6 of the slate's 17 games. Play-by-play wall clocks are decisive: Harper walked 04:03Z, went **1B→3B on a Wild Pitch 04:12Z, Caught Stealing Home 04:13Z** — the write landed 04:15:13Z, next tick; Carroll's look-alike WP advance ran 04:23-04:29Z, AFTER the write. Harper's final line 0-2, 3 BB, SB 0, **CS 1**: the initially-credited steal in that scramble was consolidated into the WP advance. Root-cause chain (guard entrenchment → 5-5 hits-tiebreak flip → Monday settle correction) unchanged. Lesson, again the standing one: I estimated the inning's wall-clock instead of reading `about.endTime` and stopped at the first mechanism that fit — timestamp the play against the tick BEFORE naming a player | yes — user pressed on "no other games going?"; the timestamp check overturned the attribution |
